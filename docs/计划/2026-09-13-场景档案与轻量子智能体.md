# 场景档案与轻量子智能体 实施计划

> **Status:** draft — set to `approved` once the user approves the plan, and
> `completed` once every task below is checked off. Keep it as the first
> content line under the title so it stays greppable (`grep -l "Status: completed" docs/计划/`).

> Steps use checkbox (`- [ ]`) syntax for tracking; check each box off as it completes.

**Goal:** 场景升级为可自由勾选的档案（记忆 + MCP 工具集 + 技能集 + 子智能体绑定），并提供基于官方 `ctx.subagents` 的轻量人设子智能体（`~/.dsh/subagents/*.md` + `subagent_run` 工具）。
**Architecture:** 纯逻辑（选集/计划计算）与 I/O（sidecar 读写、运行时应用）分离；档案引擎经依赖注入接线到既有 mcp/skills 写通道；子智能体走官方 `ctx.subagents.start` 薄封装（spawn provider，capabilities 全开，见设计 §4）。
**Tech Stack:** TypeScript（strict）→ tsc 编译到 lib/；浏览器端 `src/client.js`（dsm-* 设计系统，sync-client 逐字节复制）；测试用 Node 内建 `node:test`（仅纯逻辑冒烟，本仓库无单测套件是文档化决定）。
**Spec:** `docs/设计/2026-09-13-场景档案与轻量子智能体-design.md`（Status: approved，Spike 结论已回填）

## Global Constraints

- 页面/用户可见名称 =「场景」；目录 `~/.dsh/scene-memory/` 不改名；内部协议 `rules-*` 保留（用户裁定 + README 既有约定）。
- 人设目录 = `~/.dsh/subagents/`，一个 `.md` 一个人设，frontmatter 可选自动派生。
- 档案三段自由搭配、均可缺省；勾选集语义：勾=启用/未勾=停用；段未定义=该域不碰；清单只列实时发现的条目（用户裁定）。
- 新代码一律落域模块（`src/rules/archive*.ts`、`src/subagents/*.ts`）；`src/index.ts` 只允许装配级小改（审查报告 Important #4 的既定方向）。
- 所有新写 op 必须走 service 级 `writeOps` 派生（C2 修复机制），禁止在 index.ts 手抄白名单。
- 运行时依赖只允许新增 `fflate` 之外零新增；类型依赖允许 devDependencies 增加 `@deepseek-ai/dsh-subagent`（仅 import type）。
- 无测试套件是文档化决定：验证 = `npm run build`（tsc strict 零错 + sync-client）+ Task 1 起的 `npm test`（node:test 冒烟，仅纯逻辑）+ Task 6 真实浏览器场景测试（用户硬性要求）。
- 本计划执行者即作者本人（同会话），UI 任务给出结构与锚点，实施时对照既有 dsm-* 页面代码适配。

## File Structure

| 文件 | 职责 |
|---|---|
| `src/rules/archive.ts`（新） | 纯逻辑：档案归一化、勾选集→应用计划、快照/恢复计划（无 I/O） |
| `src/rules/archive-engine.ts`（新） | 引擎工厂：deps 注入（sidecar 读写、tools/skills 应用与发现），产出 ops + writeOps |
| `src/subagents/service.ts`（新） | 人设发现（TTL 扫描）+ `ctx.subagents.start` 薄封装（串行队列）+ `subagent-list` op |
| `src/subagents/tools.ts`（新） | `subagent_list` / `subagent_run` 工具注册（defineTool） |
| `src/index.ts`（改，装配级） | 接线档案引擎与 subagent 服务；pre-execute 增加 subagent_run ask 门；handlers/writeOps 展开 |
| `src/client.js`（改） | 「场景记忆」页改名「场景」；场景卡片三段档案 UI（勾选器 + 模式按钮） |
| `test/archive.test.mjs`（新） | archive.ts 纯逻辑冒烟（node:test，跑编译产物 lib/rules/archive.js） |
| `package.json`（改） | `test` script；devDependencies + `@deepseek-ai/dsh-subagent` |

---

### Task 1: 档案纯逻辑 + 冒烟测试

**Files:** Create `src/rules/archive.ts`、`test/archive.test.mjs`；Modify `package.json`（test script）。
**Interfaces:** Produces（Task 2 依赖）：`SceneArchive`、`ModeSnapshot`、`ModeState`、`normalizeStringList`、`normalizeArchive`、`hasSection`、`computeApplyPlan(archive, known)`、`snapshotRuntime(tools, skills)`、`computeRestorePlan(snapshot, current)`、`ApplyPlan { tools, skills, stale }`。

- [ ] **Step 1: 写 `src/rules/archive.ts`（完整实现）**

```ts
// src/rules/archive.ts —— 场景档案数据模型与模式切换纯逻辑（无 I/O；I/O 由 archive-engine 注入）。
// 勾选集语义（设计 §2.1）：段内存储「勾选的启用集合」；应用时该域启停置为与勾选集完全一致；
// 段未定义 = 该域不碰；段已定义但全不勾 = 合法（全部停用）。清单只对"实时发现"的条目生效。

export interface SceneArchive {
  tools?: string[]      // 勾选的工具选集，key = `<serverName>/<toolName>`
  skills?: string[]     // 勾选的技能选集，key = `<rootKey>/<name>`
  subagents?: string[]  // 绑定的人设名清单
}

export interface ModeSnapshot { tools: Record<string, boolean>; skills: Record<string, boolean> }
export interface ModeState { scene: string | null; snapshot: ModeSnapshot | null }

export interface ApplyPlan {
  tools: Record<string, boolean> | null   // null = 该域不碰
  skills: Record<string, boolean> | null
  stale: string[]                          // 勾选集中已不存在的条目（跳过 + 上报，不算失败）
}

export function normalizeStringList(raw: unknown): string[] {
  if (!Array.isArray(raw)) return []
  return [...new Set(raw.map((x) => String(x).trim()).filter(Boolean))]
}

/** 'tools'/'skills'/'subagents' 键存在才归一——存在性独立于集合是否为空。 */
export function normalizeArchive(raw: unknown): SceneArchive {
  const obj = (raw && typeof raw === 'object' ? raw : {}) as Record<string, unknown>
  const out: SceneArchive = {}
  if ('tools' in obj) out.tools = normalizeStringList(obj.tools)
  if ('skills' in obj) out.skills = normalizeStringList(obj.skills)
  if ('subagents' in obj) out.subagents = normalizeStringList(obj.subagents)
  return out
}

export function hasSection(archive: SceneArchive, section: 'tools' | 'skills'): boolean {
  return Array.isArray(archive[section])
}

/** 进入模式的应用计划：已定义段 → 目标 = 勾选集（仅已知条目），stale 跳过上报；未定义段 = null。 */
export function computeApplyPlan(archive: SceneArchive, known: { tools: Set<string>; skills: Set<string> }): ApplyPlan {
  const stale: string[] = []
  const plan: ApplyPlan = { tools: null, skills: null, stale }
  if (hasSection(archive, 'tools')) {
    const target: Record<string, boolean> = {}
    for (const key of known.tools) target[key] = archive.tools!.includes(key)
    for (const key of archive.tools!) if (!known.tools.has(key)) stale.push('tools/' + key)
    plan.tools = target
  }
  if (hasSection(archive, 'skills')) {
    const target: Record<string, boolean> = {}
    for (const key of known.skills) target[key] = archive.skills!.includes(key)
    for (const key of archive.skills!) if (!known.skills.has(key)) stale.push('skills/' + key)
    plan.skills = target
  }
  return plan
}

export function snapshotRuntime(tools: Record<string, boolean>, skills: Record<string, boolean>): ModeSnapshot {
  return { tools: { ...tools }, skills: { ...skills } }
}

/** 退出模式：快照键按快照值还原；快照之后新出现的键保持现状（快照不含 → 不属于被模式改动的集合）。 */
export function computeRestorePlan(snapshot: ModeSnapshot, current: { tools: Record<string, boolean>; skills: Record<string, boolean> }): ApplyPlan {
  const tools: Record<string, boolean> = {}
  for (const [key, on] of Object.entries(snapshot.tools)) tools[key] = on
  for (const [key, on] of Object.entries(current.tools)) if (!(key in tools)) tools[key] = on
  const skills: Record<string, boolean> = {}
  for (const [key, on] of Object.entries(snapshot.skills)) skills[key] = on
  for (const [key, on] of Object.entries(current.skills)) if (!(key in skills)) skills[key] = on
  return { tools, skills, stale: [] }
}
```

- [ ] **Step 2: 写 `test/archive.test.mjs`（node:test，跑编译产物）**

```js
// test/archive.test.mjs —— 场景档案纯逻辑冒烟（node --test）。跑 lib 编译产物，改 src 后先 npm run build。
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { normalizeArchive, hasSection, computeApplyPlan, snapshotRuntime, computeRestorePlan } from '../lib/rules/archive.js'

test('normalizeArchive: 键存在性独立于集合空否', () => {
  const a = normalizeArchive({ tools: [], subagents: 'x, y' })
  assert.deepEqual(a.tools, [])
  assert.equal(a.skills, undefined)
  assert.deepEqual(a.subagents, ['x', 'y'])
  assert.equal(hasSection(a, 'tools'), true)
  assert.equal(hasSection(a, 'skills'), false)
})

test('computeApplyPlan: 勾选集→目标启停，stale 跳过，未定义段不碰', () => {
  const known = { tools: new Set(['a/t1', 'a/t2', 'b/t3']), skills: new Set(['dsh/s1', 'dsh/s2']) }
  const plan = computeApplyPlan(normalizeArchive({ tools: ['a/t1', 'gone/t9'] }), known)
  assert.deepEqual(plan.tools, { 'a/t1': true, 'a/t2': false, 'b/t3': false })
  assert.equal(plan.skills, null)
  assert.deepEqual(plan.stale, ['tools/gone/t9'])
})

test('computeApplyPlan: 段全不勾 = 全停（合法），两段同时应用', () => {
  const known = { tools: new Set(['a/t1']), skills: new Set(['dsh/s1']) }
  const plan = computeApplyPlan(normalizeArchive({ tools: [], skills: ['dsh/s1'] }), known)
  assert.deepEqual(plan.tools, { 'a/t1': false })
  assert.deepEqual(plan.skills, { 'dsh/s1': true })
})

test('restore: 快照键按快照值还原，快照后新增键保持现状', () => {
  const snapshot = snapshotRuntime({ 'a/t1': true, 'a/t2': false }, { 'dsh/s1': true })
  const plan = computeRestorePlan(snapshot, { tools: { 'a/t1': false, 'new/t': true }, skills: { 'dsh/s1': false } })
  assert.deepEqual(plan.tools, { 'a/t1': true, 'a/t2': false, 'new/t': true })
  assert.deepEqual(plan.skills, { 'dsh/s1': true })
})
```

- [ ] **Step 3: package.json 加 test script**：`"test": "npm run build && node --test test/"`（加在 `"lint"` 旁）。
- [ ] **Step 4: 跑 `npm test` 看通过**（先 build 后 4 个用例全绿；如有红，修实现而非断言）。
- [ ] **Step 5: Commit**：`git add -A && git commit -m "feat(scenes): 档案纯逻辑——勾选集/应用计划/快照恢复 + node:test 冒烟"`

### Task 2: 档案引擎 + ops + 门禁派生

**Files:** Create `src/rules/archive-engine.ts`；Modify `src/index.ts`（装配 + handlers + 依赖注入适配）、`src/rules/service.ts`（新增 `rules-index-patch` 读/写 mode+archives 切片的透传 op，复用既有 index 读写函数）。
**Interfaces:** Consumes：Task 1 全部；既有 `mcpmToolEnabled`（index.ts，单个工具启停写）、`readDisabledTools`/disabledTools 缓存、`skillsService.ops['skill-state']`。Produces：`archiveService = { ops: Record<string,op>, writeOps: ReadonlySet<string> }`，op：`scene-archive-save{scene, archive}` / `scene-mode-set{scene: string|null}` / `scene-mode-get{}`；handlers 表展开 `...archiveService.ops`，`WRITE_OPS` 展开 `...archiveService.writeOps`。

- [ ] **Step 1: 写 `src/rules/archive-engine.ts`**（deps 注入工厂，完整实现）

```ts
// src/rules/archive-engine.ts —— 场景档案引擎：sidecar 读写 + 运行时应用，全部经 deps 注入（无直接 I/O）。
import { computeApplyPlan, computeRestorePlan, normalizeArchive, snapshotRuntime, hasSection,
  type ApplyPlan, type ModeState, type SceneArchive } from './archive.js'

export interface ArchiveIndexSlice { archives: Record<string, SceneArchive>; mode: ModeState }

export interface ArchiveEngineDeps {
  /** 读 rules-index.json 的 mode/archives 切片（由 rules service 提供，保持其唯一属主）。 */
  loadSlice(): Promise<ArchiveIndexSlice>
  /** 写回切片（合并进 rules-index.json，rules service 负责 .bak/原子性）。 */
  saveSlice(slice: ArchiveIndexSlice): Promise<void>
  /** 实时发现的工具 key 全集（含启停 sidecar 中已知的 key）。 */
  knownToolKeys(): Promise<Set<string>>
  /** 实时发现的技能 key 全集（key = `<rootKey>/<name>`）。 */
  knownSkillKeys(): Promise<Set<string>>
  /** 当前工具启停全集。 */
  currentTools(): Promise<Record<string, boolean>>
  /** 当前技能启停全集。 */
  currentSkills(): Promise<Record<string, boolean>>
  /** 单域批量应用（引擎保证串行；实现复用既有单条写通道）。 */
  applyTools(target: Record<string, boolean>): Promise<void>
  applySkills(target: Record<string, boolean>): Promise<void>
  /** 场景名是否真实存在（进入模式前校验）。 */
  sceneExists(name: string): Promise<boolean>
}

export interface ArchiveEngine {
  ops: Record<string, (args: any) => Promise<any>>
  writeOps: ReadonlySet<string>
}

export function createArchiveEngine(deps: ArchiveEngineDeps): ArchiveEngine {
  let chain: Promise<unknown> = Promise.resolve()
  const serial = <T>(task: () => Promise<T>): Promise<T> => {
    const queued = chain.then(task, task)
    chain = queued.catch(() => undefined)
    return queued
  }

  async function applyPlan(plan: ApplyPlan): Promise<void> {
    if (plan.tools) await deps.applyTools(plan.tools)
    if (plan.skills) await deps.applySkills(plan.skills)
  }

  const ops = {
    // 读：页面渲染模式与档案
    'scene-mode-get': async () => ({ ok: true, ...(await deps.loadSlice()) }),

    // 写：保存某场景档案（勾选集整体替换；键对实时发现全集校验，未知键丢弃并在结果中报告）
    'scene-archive-save': (args: any) => serial(async () => {
      const scene = String((args && args.scene) || '').trim()
      if (!scene) return { ok: false, error: '缺少场景名' }
      if (!(await deps.sceneExists(scene))) return { ok: false, error: `场景不存在: ${scene}` }
      const raw = (args && args.archive) || {}
      const archive = normalizeArchive(raw)
      const stale: string[] = []
      const knownT = await deps.knownToolKeys()
      if (archive.tools) { const keep = archive.tools.filter((k) => knownT.has(k)); stale.push(...archive.tools.filter((k) => !knownT.has(k)).map((k) => 'tools/' + k)); archive.tools = keep }
      const knownS = await deps.knownSkillKeys()
      if (archive.skills) { const keep = archive.skills.filter((k) => knownS.has(k)); stale.push(...archive.skills.filter((k) => !knownS.has(k)).map((k) => 'skills/' + k)); archive.skills = keep }
      const slice = await deps.loadSlice()
      if (Object.keys(archive).length === 0) delete slice.archives[scene]
      else slice.archives[scene] = archive
      await deps.saveSlice(slice)
      return { ok: true, scene, archive: slice.archives[scene] ?? null, stale }
    }),

    // 写：进入/退出/切换当前模式（状态机见设计 §2.2；失败回滚 fail-closed）
    'scene-mode-set': (args: any) => serial(async () => {
      const target = args && 'scene' in (args || {}) ? (args.scene == null ? null : String(args.scene).trim() || null) : undefined
      if (target === undefined) return { ok: false, error: '缺少 scene（null=退出模式）' }
      const slice = await deps.loadSlice()
      const current = slice.mode ?? { scene: null, snapshot: null }
      if (target === current.scene) return { ok: true, mode: current, applied: null, stale: [] }
      const stale: string[] = []
      // 1) 退出当前模式（有快照才恢复）
      if (current.scene && current.snapshot) {
        const restore = computeRestorePlan(current.snapshot, {
          tools: await deps.currentTools(),
          skills: await deps.currentSkills(),
        })
        await applyPlan(restore)
      }
      // 2) 进入新模式
      if (target) {
        const archive = slice.archives[target]
        if (!archive || (!hasSection(archive, 'tools') && !hasSection(archive, 'skills'))) {
          // 无 tools/skills 段：无可应用档案——允许仅作记忆收窄的模式（记录 mode 但不快照）
          const mode: ModeState = { scene: target, snapshot: null }
          await deps.saveSlice({ ...slice, mode })
          return { ok: true, mode, applied: null, stale }
        }
        if (!(await deps.sceneExists(target))) return { ok: false, error: `场景不存在: ${target}` }
        const snapshot = snapshotRuntime(await deps.currentTools(), await deps.currentSkills())
        const plan = computeApplyPlan(archive, { tools: await deps.knownToolKeys(), skills: await deps.knownSkillKeys() })
        stale.push(...plan.stale)
        try {
          await applyPlan(plan)
        } catch (e) {
          // fail-closed：应用失败 → 立即按快照回滚再报错
          await applyPlan(computeRestorePlan(snapshot, { tools: await deps.currentTools(), skills: await deps.currentSkills() })).catch(() => {})
          return { ok: false, error: '应用档案失败，已回滚: ' + String((e && (e as Error).message) || e) }
        }
        const mode: ModeState = { scene: target, snapshot }
        await deps.saveSlice({ ...slice, mode })
        return { ok: true, mode, applied: { tools: !!plan.tools, skills: !!plan.skills }, stale }
      }
      const mode: ModeState = { scene: null, snapshot: null }
      await deps.saveSlice({ ...slice, mode })
      return { ok: true, mode, applied: null, stale }
    }),
  }
  return { ops, writeOps: new Set(['scene-archive-save', 'scene-mode-set']) }
}
```

- [ ] **Step 2: index.ts 装配**（rules service 块之后，WRITE_OPS 构造之前）：
  1. 读 `index.ts` 中 `mcpmToolEnabled`（约 :1066）的参数形状，写适配器 `applyToolOne(key, on)`（key 按 `<server>/<tool>` 拆参）与 `readToolStates()`（从既有 disabledTools 读取/缓存，缺省 true）；
  2. `knownToolKeys()` 复用 `mcpmTools`（约 :1185）的数据源取 live 工具 key 并集 ∪ 启停表键集；`knownSkillKeys()`/`currentSkills()` 由 `skillsService.ops['skill-state']` 结果展平（`root.key + '/' + skill.name`，enabled !== false）；`sceneExists` 用 `rulesService.ops['rules-list']` 的 scenes；
  3. `loadSlice/saveSlice`：`rulesService` 需要新增两个透传 op（`rules-index-patch` 读/写 mode+archives 切片，内部复用其既有 index 读写函数并保留 .bak/原子写语义）——在 `src/rules/service.ts` 增加 `ops['rules-index-patch']` 与返回对象上的 `patchIndex` 方法；
  4. `const archiveService = createArchiveEngine({...})`；handlers 表加 `...archiveService.ops`；`WRITE_OPS` 加 `...archiveService.writeOps`。
- [ ] **Step 3: `npm run build` + `npm run lint` 零错**。
- [ ] **Step 4: 运行验证（真实 DSH）**：起 `dsh web`，浏览器 `fetch` 断言 `scene-mode-get` 返回 `{archives:{},mode:{scene:null,snapshot:null}}`；`scene-archive-save` 写入/读回一致；`scene-mode-set` 对不存在场景返回 `{ok:false}`。
- [ ] **Step 5: Commit**：`git commit -am "feat(scenes): 档案引擎+ops（勾选集应用/快照回滚/门禁派生）"`

### Task 3: 子代理服务（人设发现 + ctx.subagents 薄封装）

**Files:** Create `src/subagents/service.ts`；Modify `package.json`（devDependencies + `@deepseek-ai/dsh-subagent`，`npm install`）。
**Interfaces:** Produces：`createSubagentService(ctx, { subagentsDir }) → { list(), ops, runSerial(parentAgent, persona, task, signal) }`；`PersonaDoc { name, description, model?, tools?, body, path }`；op `subagent-list{}`（只读）。

- [ ] **Step 1: `npm i -D @deepseek-ai/dsh-subagent@^0.1.5-rc.2`**（版本对齐 peerDependencies 的 rc.2 线）。
- [ ] **Step 2: 写 `src/subagents/service.ts`**（完整实现，核心如下）

```ts
// src/subagents/service.ts —— 轻量子智能体：人设发现（TTL 扫描）+ 官方 ctx.subagents.start 薄封装。
import { readdir, readFile } from 'node:fs/promises'
import { join } from 'node:path'

export interface PersonaDoc {
  name: string; description: string; model?: string; tools?: string[]
  body: string; path: string
}

/** 行式 frontmatter 解析：只认 description / model / tools 三个键（人设文件不需要完整 YAML）。 */
function parsePersona(raw: string, fallbackName: string): PersonaDoc {
  const m = /^---\r?\n([\s\S]*?)\r?\n---\r?\n?/.exec(raw)
  const data: Record<string, string> = {}
  let body = raw
  if (m) {
    for (const line of m[1].split(/\r?\n/)) {
      const kv = /^([A-Za-z_-]+)\s*:\s*(.*)$/.exec(line.trim())
      if (kv) data[kv[1].toLowerCase()] = kv[2].trim()
    }
    body = raw.slice(m[0].length)
  }
  const tools = data.tools ? data.tools.split(/[,，]/).map((s) => s.trim()).filter(Boolean) : undefined
  const firstLine = body.split(/\r?\n/).find((l) => l.trim())?.trim() ?? ''
  return {
    name: fallbackName,
    description: data.description || firstLine,
    model: data.model || undefined,
    tools: tools && tools.length ? tools : undefined,
    body: body.trim(),
    path: '',
  }
}

const RESULT_MAX = 16 * 1024

export function createSubagentService(ctx: any, opts: { subagentsDir: string }) {
  let cache: { at: number; value: PersonaDoc[] } | null = null
  async function list(): Promise<PersonaDoc[]> {
    if (cache && Date.now() - cache.at < 1000) return cache.value
    let docs: PersonaDoc[] = []
    try {
      const entries = await readdir(opts.subagentsDir).catch(() => [] as string[])
      for (const name of entries.filter((e) => e.endsWith('.md')).sort()) {
        const raw = await readFile(join(opts.subagentsDir, name), 'utf8').catch(() => '')
        if (!raw.trim()) continue
        docs.push({ ...parsePersona(raw, name.slice(0, -3)), path: join(opts.subagentsDir, name) })
      }
    } catch { /* 目录缺失 = 无人设 */ }
    cache = { at: Date.now(), value: docs }
    return docs
  }

  // v1 串行：同一时刻至多一个子代理运行（设计 §3.2）。
  let chain: Promise<unknown> = Promise.resolve()
  const runSerial = (parentAgent: any, p: PersonaDoc, task: string, signal: AbortSignal) => {
    const queued = chain.then(() => runOnce(parentAgent, p, task, signal), () => runOnce(parentAgent, p, task, signal))
    chain = queued.catch(() => undefined)
    return queued
  }

  async function ensureSpawnProvider(): Promise<any> {
    const runtime = ctx.get?.('subagents')
    if (!runtime) throw new Error('子代理服务未挂载（ctx.subagents 缺失；请确认 DSH 版本 ≥0.1.5-rc.2）')
    try {
      const providers = typeof runtime.listProviders === 'function' ? runtime.listProviders() : []
      const names: string[] = Array.isArray(providers) ? providers.map((x: any) => x?.name ?? x) : []
      if (names.length && !names.includes('spawn')) {
        const mod = await import('@deepseek-ai/dsh-subagent-spawn-in-process')
        mod.apply(ctx, { providerName: 'spawn' })
      }
    } catch { /* 探测失败不阻断——start 时由 provider 缺失报错兜底 */ }
    return runtime
  }

  async function runOnce(parentAgent: any, p: PersonaDoc, task: string, signal: AbortSignal): Promise<{ text: string; runId: string; stopReason: string }> {
    const runtime = await ensureSpawnProvider()
    const run = await runtime.start({
      provider: 'spawn',
      label: p.name,
      parent: parentAgent,
      signal,
      prompt: [{ type: 'text', text: task }],
      persona: p.body,
      ...(p.tools?.length ? { toolFilter: { allow: p.tools } } : {}),
      maxDepth: 1,
      ...(p.model ? { agentOptions: { model: p.model } } : {}),
    })
    try {
      const result = await run.result   // 官方契约：child 级失败不 reject（stopReason 体现）
      const text = (result.output ?? [])
        .map((b: any) => (b && typeof b === 'object' && 'text' in b ? String(b.text) : ''))
        .join('').trim()
      const detail = (result as any).detail ? `\n\n[provider] ${String((result as any).detail)}` : ''
      return {
        text: ((text || '(子代理无输出)') + detail).slice(0, RESULT_MAX),
        runId: String(run.id ?? ''),
        stopReason: String((result as any).stopReason ?? 'completed'),
      }
    } finally {
      await run.dispose().catch(() => {})   // 官方前台语义：collection 后即弃
    }
  }

  const ops = {
    'subagent-list': async () => {
      const docs = await list()
      return { ok: true, subagents: docs.map((p) => ({ name: p.name, description: p.description, model: p.model ?? null, tools: p.tools ?? null })) }
    },
  }
  return { list, ops, runSerial }
}
```

- [ ] **Step 3: `npm run build` 零错**（`@deepseek-ai/dsh-subagent` 目前只在此文件以运行时 dynamic import 出现，不需要类型导入——ctx 形状用结构化 any，与 skills/service.ts 同风格）。
- [ ] **Step 4: Commit**：`git commit -am "feat(subagents): 人设发现+ctx.subagents 薄封装（串行/16KiB 截断/即用即弃）"`

### Task 4: 工具注册 + ask 门 + 场景绑定校验

**Files:** Create `src/subagents/tools.ts`；Modify `src/index.ts`（装配 + handlers 不变 + pre-execute 门 + 工具注册）。
**Interfaces:** Consumes：Task 3 全部、既有 `tools.register(defineTool(...))` 与 `tools/pre-execute` 门（index.ts :2260 起）。Produces：模型工具 `subagent_list` / `subagent_run`（工具总数 10 → 12）。

- [ ] **Step 1: 写 `src/subagents/tools.ts`**

```ts
// src/subagents/tools.ts —— 模型工具：subagent_list / subagent_run（人设清单 + 官方 ctx.subagents 委派）。
import type { ToolDefinition } from '@deepseek-ai/dsh-tools'
import type { PersonaDoc } from './service.js'

export function text(v: string) { return { type: 'text' as const, text: v } }

/** 场景绑定校验（设计 §3.3）：启用场景的 subagents 并集；空并集 = 全部可用。 */
export async function filterBySceneBinding(
  docs: PersonaDoc[],
  enabledSceneLists: string[][],
): Promise<{ allowed: PersonaDoc[]; reason: string | null }> {
  const bound = new Set(enabledSceneLists.flat())
  if (!bound.size) return { allowed: docs, reason: null }
  const allowed = docs.filter((d) => bound.has(d.name))
  return { allowed, reason: allowed.length ? null : `当前启用场景的子智能体绑定: ${[...bound].join('、')}` }
}

export function defineSubagentListTool(subagents: { list(): Promise<PersonaDoc[]>; sceneLists(): Promise<string[][]> }): ToolDefinition {
  return {
    name: 'subagent_list',
    description: 'List available personas (pre-configured subagent profiles) with their descriptions.',
    parameters: {},
    output: { schema: { type: 'string' }, render: (_a: unknown, v: string) => text(v) },
    async execute() {
      const { allowed, reason } = filterBySceneBinding(await subagents.list(), await subagents.sceneLists())
      const lines = allowed.map((p) => '- ' + p.name + ' — ' + (p.description || '(无描述)'))
      return '人设子智能体（' + allowed.length + '）：\n' + (lines.join('\n') || '(无)') + (reason ? '\n注意：' + reason : '')
    },
  }
}

export function defineSubagentRunTool(
  subagents: { list(): Promise<PersonaDoc[]>; sceneLists(): Promise<string[][]>; runSerial(parentAgent: any, p: PersonaDoc, task: string, signal: AbortSignal): Promise<{ text: string; runId: string; stopReason: string }> },
  currentAgent: () => any,
): ToolDefinition {
  return {
    name: 'subagent_run',
    description: 'Run a named persona as a one-shot subagent: it gets the persona as its own system prompt, works on `task` with a fresh context, and returns only its final output. Use subagent_list first to discover personas. Each run is stateless and ephemeral.',
    parameters: {
      agent: { type: 'string', required: true, description: 'Persona name from subagent_list.' },
      task: { type: 'string', required: true, description: 'Complete, self-contained task for the subagent (it does not see this conversation).' },
    },
    output: { schema: { type: 'string' }, render: (_a: unknown, v: string) => text(v) },
    async execute(args: any, exec: any) {
      const name = String((args && args.agent) || '').trim()
      const task = String((args && args.task) || '').trim()
      if (!task) throw new Error('task 不能为空')
      const docs = await subagents.list()
      const { allowed, reason } = filterBySceneBinding(docs, await subagents.sceneLists())
      const persona = allowed.find((p) => p.name === name)
      if (!persona) throw new Error('人设不可用: ' + name + (reason ? '（' + reason + '）' : '（可用: ' + allowed.map((p) => p.name).join('、') + '）'))
      const r = await subagents.runSerial(currentAgent(), persona, task, (exec && exec.signal) as AbortSignal)
      const prefix = r.stopReason && r.stopReason !== 'completed' ? `[stopReason: ${r.stopReason}]\n` : ''
      return prefix + r.text
    },
  }
}
```

- [ ] **Step 2: index.ts 装配**（subagent 服务与工具）：
  1. `const subagentService = createSubagentService(ctx, { subagentsDir: join(HOME, 'subagents') })`（HOME 取既有路径解析；与 rulesRoot 同源）；handlers 表加 `'subagent-list': ...`（只读 op）；
  2. 工具注册（:2064 工具区）：`tools.register(defineSubagentListTool(...))`、`tools.register(defineSubagentRunTool(...))`；`currentAgent` 从调用上下文取——工具 execute 无 agent 参数时降级 `ctx.get('agents')` 当前活动 agent 或 `undefined`（spawn provider 以 parent 推导 cwd/深度；undefined 时报结构化错误）——**实施时对照 defineTool execute 的第二参（exec）实际形状适配**；
  3. `sceneLists()` 适配：`rulesService.ops['rules-list']({})` 的 enabled 场景 → 各场景 `archives[scene]?.subagents ?? []`；
  4. pre-execute 门（:2261 块内追加一个分支）：`if (exec?.name === 'subagent_run') return readPluginSettings().then((s) => s.requireConfirmForModelSubagentRun ? { kind: 'ask', reason: 'Run a subagent (consumes tokens)' } : next()).catch(() => ({ kind: 'ask', reason: 'Run a subagent (consumes tokens)' }))`；设置默认值 true（`readPluginSettings` 缺该键时按 true 处理）。
- [ ] **Step 5: `npm run build` + `npm run lint` 零错**。
- [ ] **Step 6: Commit**：`git commit -am "feat(subagents): subagent_list/subagent_run 工具 + ask 门 + 场景绑定校验"`

### Task 5: client.js 场景页改造（改名 + 勾选器 + 模式按钮）

**Files:** Modify `src/client.js`（场景记忆页函数区，约 :2016 起）。
**Interfaces:** Consumes：HTTP op `scene-mode-get/scene-archive-save/scene-mode-set`、`subagent-list`。Produces：页面标题「场景」、卡片三段档案 UI。

- [ ] **Step 1: 页面更名**：页面标题/文案「场景记忆」→「场景」（保留 op 名 `/scene-memory` 与数据目录不变）；对照既有页面 header 写法同步 DICT 文案键。
- [ ] **Step 2: 档案区 UI**（对照既有场景卡片代码插入）：
  - 卡片新增「档案」折叠区：三个可添加段按钮 `+ 工具集`、`+ 技能集`、`+ 子智能体`（已定义的段直接渲染勾选列表 + 移除段按钮）；
  - 勾选器数据源：`mcpm-tools`（工具 key=`server/tool`，只列 live 服务）、`skill-state`（key=`root/name`）、`subagent-list`；**预勾当前启用状态**（工具取启停表，技能取 enabled，子智能体取档案现值）；搜索框过滤；
  - 「保存到场景」→ `scene-archive-save{scene, archive}`（含段存在性：勾过至少一次才写入该段键）；「移除段」= 保存时不含该键；
- [ ] **Step 3: 模式按钮**：档案含 tools/skills 段的卡片显示 `设为当前模式`（互斥，请求 `scene-mode-set{scene}`）；模式中卡片常驻标记 + `退出模式`（`scene-mode-set{scene:null}`）；`scene-mode-get` 驱动页头状态条（当前模式 + 失效项 stale 展示）。
- [ ] **Step 4: 反馈与错误**：操作结果沿用该页现有 msg/toast 通道；`{ok:false}` 原样展示 error。
- [ ] **Step 5: `npm run build` + `npm run lint` 零错**（client.js 经 sync-client 复制）。
- [ ] **Step 6: Commit**：`git commit -am "feat(scenes-ui): 场景页档案三段勾选器 + 当前模式按钮 + 更名"`

### Task 6: 文档 + 真实浏览器场景测试 + 收尾

- [ ] **Step 1: README 更新**：核心亮点表加「场景档案」「子智能体」两行；「管场景记忆」章节改「管场景」；数据落点表加 `~/.dsh/subagents/` 与 rules-index 新键；模型工具数 10 → 12。
- [ ] **Step 2: 设计文档状态复核 + 文档导航登记本计划**。
- [ ] **Step 3: 真实浏览器场景测试（`dsh web` + control-browser，延续既有方法）**：
  1. 建人设 `echo-agent.md`（正文要求：复述任务并统计字数）→ 聊天里让主模型 `subagent_list` + `subagent_run{agent:'echo-agent', task:'...'}` → 核对返回文本与 ask 门弹窗；
  2. 场景档案：给测试场景勾选工具集/技能集/子智能体 → 「设为当前模式」→ MCP/Skills 页核对启停已切换 → 退出模式核对恢复 → 「保存到场景」核对回写；
  3. 绑定校验：场景只绑 `echo-agent` 时，请求另一人设返回结构化错误；
  4. token 门禁：新写 op `scene-archive-save`/`scene-mode-set` 无 token 403 文案；
  5. S3 验证：echo-agent 人设正文要求"复述你 system prompt 中'场景记忆'段的第一行"——能复述 = 继承成立；不成立则按设计回退（把活动场景记忆文本块拼进 task 前部）并回填设计文档；
  6. 测试数据全清理（人设/测试场景/模式复位）。
- [ ] **Step 4: 全绿后**：本计划 Status → completed；日志 `docs/日志/`；按 executing-plans 收尾链（code-review → verification-before-completion → finishing-a-development-branch）。
