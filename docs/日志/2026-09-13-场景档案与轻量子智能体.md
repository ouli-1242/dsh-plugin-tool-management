# 场景档案与轻量子智能体 开发日志

**Status:** completed

- **日期**：2026-09-13
- **分支**：`fix/review-2026-09-13`（设计/计划/实现同分支推进）
- **设计**：`docs/设计/2026-09-13-场景档案与轻量子智能体-design.md`（approved）
- **计划**：`docs/计划/2026-09-13-场景档案与轻量子智能体.md`（completed）

## 今日做了什么（含提交 SHA）

1. 设计阶段：三方案对话收敛（钩子不做、命令不做、场景=记忆+MCP+Skills+子智能体绑定；勾选集语义、单一当前模式）；四项 Spike 全部有结论——S1/S2/S4 官方解（`ctx.subagents.start(name, request)` 即创建+投递、`run.result` 收尾、spawn provider capabilities 全开含 persona/toolFilter），S3 双路径。设计 approved（acc45ac / 75509ab / 8f0caef / acc45ac）。
2. 实施计划落盘（d63de46），六任务按序执行：
   - Task 1 `14f0201`：`src/rules/archive.ts` 勾选集纯逻辑 + 首个 `node:test` 冒烟（4/4 绿）。
   - Task 2 `b04b486`：`src/rules/archive-engine.ts` 引擎 + `rules-index.json` 扩展（mode/archives）+ rules service `patchIndex/readArchiveSlice` + index.ts 注入既有写通道；`b8bf184` 删场景时联动清理档案与模式引用。
   - Task 3+4 `02fff53`：`src/subagents/service.ts`（人设发现 + start 薄封装 + 串行 + 16KiB 截断）+ `src/subagents/tools.ts`（subagent_list/run）+ ask 门（`requireConfirmForModelSubagentRun`，默认 true，`293de7d` 补设置读写）。
   - Task 5 `f7e78b1`：场景页更名「场景」+ 档案三段勾选器 + 当前模式按钮/标记 + `scene-inventory` 聚合 op。
   - 修复：`start(name, request)` 官方双参签名（d63de46 后）、refresh 改合并式更新（修「场景」tab 白屏）。
3. Task 6：README 更新（场景档案/子智能体/工具 12 个/数据落点）；真实浏览器场景测试（见证据）。

## 证据（新鲜运行）

- `npm run build`（tsc strict）+ `npm run lint` + `npm test`：4/4 通过（本轮全部提交后重新运行）。
- 真实 `dsh web`（web profile junction 挂载本开发目录）+ 浏览器实测：
  - **档案**：`scene-archive-save` 带失效键 → `stale` 上报并丢弃；进模式 `applied:{tools:true}` 并捕获真实快照；**github 工具 26/26 → 模式中 0/26 → 退出 26/26 恢复**；UI 点「设为当前模式」→ 卡片标记 + 页头「当前模式: test-scene」+ API 侧 0/26。
  - **子智能体**：模型真实调用 `subagent_list`（返回绑定过滤后清单，绑定外 other-agent 不出现）→ `subagent_run echo-agent` **成功**，任务要求复述「场景记忆」段首行，子代理返回「这是 S3 继承验证用的场景记忆首行。」→ **S3：子代理继承场景记忆段，实锤**；右侧任务管理出现子代理节点。
  - **绑定拒绝**：`subagent_run other-agent` → `Error: 人设不可用: other-agent（可用: echo-agent`，稳定复现。
  - **ask 门**：完全权限会话中确认弹窗按官方语义 fail-closed 自动拒绝（×3 复现）；`requireConfirmForModelSubagentRun:false` 时放行（设置通道验证）；新写 op 无 token 403 文案正常。

## 偏离声明（实际 vs 计划）

- Task 3 Step 1 的 devDep `@deepseek-ai/dsh-subagent` 未安装——实现用结构化 any + `createRequire`（运行时挂载宿主侧 spawn provider），无需类型依赖。
- spawn provider 挂载改为**惰性探测 + createRequire**（计划中的 cordis.patch.yml 挂载行未用——探测到缺失才挂，避免与宿主默认组合双挂载）。
- `subagent_list` 的 `parameters` 必须可生成 `type:object` schema：空对象会产出 `type:null` 被模型 API 拒绝——加了可选 `scene` 参数（计划未预见）。
- `ctx.subagents.start` 实际签名为 `start(name, request)` 双参（计划误写单对象参数）。
- 删除场景时联动清理档案/模式引用（计划未覆盖的边界，运行测试发现）。
- tsc incremental（tsbuildinfo）曾导致 lib/index.js 未重编译（工具 schema 改动未生效）——重建时删除 tsbuildinfo 强制全量；后续构建如遇"改了没生效"先清它。


## UI 重构 v2（用户反馈第二轮，f726989 / f726989 后续）

- 七栏重构：场景/MCP/技能/子智能体/提示词/记忆/会话（用户指定顺序）；AGENTS.md→提示词、History→会话。
- 场景页管编排、记忆页管内容、子智能体页管人设 CRUD（subagent-create/update/delete/get，writeOps 派生进门禁）。
- 档案 MCP 段两级：服务器勾选（全部展示含未运行）+ 最右「选工具」钻取；数据模型 v2 mcp: Record<server, '*' | string[]>；通配由 guard/restrict 原生支持（isToolDisabled + restrict 展开）。
- 非干扰原则成文（设计 §9）：全部状态在插件自有命名空间，DSH 侧仅走官方扩展缝。
- 修复：档案弹窗三段缺「添加段」按钮、zh 字典丢 memory.archive.save/addTools 键、openDrill 引用已删 state（drill 永不打开）、refresh 整对象替换抹状态（白屏根因）、客户端 null 段泄漏为空段定义（服务端归一化双保险）。

## 诚实边界（未做 / 未验证 / 受阻）

- 测试会话为「完全权限 + 审批禁用」模式，ask 门只验证到 fail-closed 自动拒绝分支；**确认弹窗的人工点击放行分支未在浏览器实测**（逻辑与 rule_manager_write 同一通道，风险低）。
- `subagent_fork`（官方另一委派形态）与 continuable 子代理未接入（设计 v1 范围外）。
- 档案应用对 N 个工具/技能是逐条写（N 次文件写，写锁内串行）——量大时可优化为一次原子写，v1 未做。
- 本轮功能性 diff 未跑正式 code-review 双轴评审（立即下一步）；浏览器测试数据已全部清理（scene-memory 空、settings 文件删除、rules-index 归零、github 工具 26/26 启用）。
- UI v2 轮实测覆盖：七栏渲染、场景建删、档案三段（MCP 两级含未运行服务器、钻取勾选、保存读回一致）；子智能体页 CRUD 与聊天链路沿用前轮证据（本轮未重跑聊天端到端）。

## v2.1 增量：导入 + 全局记忆（2026-09-13 第二段，同日实现并验证）

用户追加四条：子智能体介绍一句话、子智能体可导入、记忆可导入、记忆可不选场景（全局、任何对话都注入、默认不选）。

**做了什么**

1. `src/imports/upload.ts`：导入共享纯逻辑（base64 → ZIP 魔数识别/解包 → 路径校验 → 人设/记忆落点规划）。`test/import.test.mjs` 9 例。
2. 两条导入通道：`subagent-import`（人设，`.md`/`.zip`，只看文件名）、`rules-import`（记忆，zip 内目录 = 场景、裸 `.md` = 弹窗所选场景）。两者都进门禁 `writeOps`，重名一律**跳过并报告**。
3. UI：模块级 `ImportModal`（拖放/多选/已选计数/清空/安装）被子智能体页与记忆页共用；记忆页额外带「导入到场景」字段（默认留空）。
4. **全局记忆修复**（用户第 4 条的真实拦路石，三处）：`resolveActiveScenes` 把根层 `''` 纳入活动集合；`rules-create` 允许空场景并把 id 改为与 `parseId` 同构（原先会写出 `/名字` 悬空 id）；网页端新建按钮去掉「场景非空」硬条件。
5. 文案收口：子智能体页介绍改一句话；人设「描述」提示改「一句话即可」；记忆场景字段/全局卡片/场景选择器补「留空 = 全局（任何对话都注入）」。

**证据（新鲜运行）**

- `npm test`：**22/22 通过**（archive 13 + import 9）；`npm run lint` 通过；i18n 双语各 395 键、无重复、无单语键。
- 浏览器实测（`dsh web` + playwright-cli，真实磁盘三处交叉核对）：
  - **子智能体导入**：导入 `code-review.md` → 「已导入 1 个人设：code-review」，列表出现该人设；重复导入 → 「没有导入任何文件（全部被跳过）· 已跳过：code-review（同名已存在（已跳过））」；磁盘 `~/.dsh/subagents/code-review.md` 落盘。
  - **记忆导入（单文件）**：场景留空 → 「已导入 1 条记忆：loose」，落 `scene-memory/loose.md`，索引 id 为裸名 `loose`（非 `/loose`）。
  - **全局注入（关键断言）**：仅此一条无场景记忆时 `rules-budget.usedBytes` 由 **0 → 155**（修复前恒为 0：根层记忆从不注入）。
  - **记忆导入（zip）**：`工作/standup.md` + `global-note.md` → 「已导入 2 条记忆：工作/standup、global-note」；场景页出现「工作」卡片，全局卡片出现 `global-note`；`usedBytes 379`、`items 3`。
  - **空场景新建记忆**：场景字段默认空、填名与正文后「创建」可点（修复前恒 disabled）→ 落 `scene-memory/pw-create.md`，索引裸 id，全局卡片 +1，`usedBytes 379 → 511`。
  - **重名策略**：记忆重复导入同样「同名已存在（已跳过）」，原文件未被改动。
- 测试数据与进程已清理：`scene-memory/` 空、`subagents/` 空、`rules-index.json` 回到 `{rules:{},groups:{},active:null,archives:{},mode:{scene:null,snapshot:null}}`；自建 `dsh web` 实例（8123/8124）已停止；临时脚本与夹具已删除。

**诚实边界**

- bundle（`<场景>/<名>/SKILL.md` + 附件）**不支持导入**：zip 内 `SKILL.md` 条目跳过并回报（本轮口径，未选 bundle 选项）。
- 导入的限额（8 MiB/文件、32 MiB/批、200 文件/批、2000 zip 条目）只在单测与代码层核对，**未做大文件实测**。
- `subagent_run` 端到端（导入的人设真正跑一次）**未跑**——本轮只验证到人设清单与文件落盘。
- 拖放路径（`dropzone`）只做了代码复用（与技能导入同一 `droppedFiles` 工具），**未在浏览器实拖**；实测走的是文件选择器。

## 评审修复 + C/D 批（UI 统一、技能同名自选）+ 收尾复验（2026-09-13 第三段）

接续双轴评审：24 条 + 实测新增 C6 全部代码级处置（权威记录见审查文档 §5），随后同日完成两批增量。交接快照见 `docs/交接/2026-09-13-交接-评审修复与UI统一.md`。

**做了什么**

1. **C 批 UI 统一 + 档案编辑器重做**：七页统一段卡片 `.dsm-seg`（标题栏 + 216px 固定滚动体 + 脚注）；档案弹窗固定高度（加/删段、进出「选工具」恒 620px，实测核对）；「选工具」改段内切换；每段 全选/清空；顶部勾选摘要 + 语义说明；Notice 统一（成功=浮层、警告/错误=文档流横幅，七页全走）；页头同构 + 场景页三格统计；侧栏 `TOOLS`→`工具`；七页描述统一单句（中英）。MCP 页硬编码中文为历史遗留，本轮只统一结构。
2. **D 批技能同名自选**：`preferredSkills` 首选表（声明名→来源 key）+ `groupLoadableSkillsByName(items, preferred)` 导出 + `skill-prefer`/`skill-unprefer` op（进 writeOps 门禁，只写本地策略）；前端被覆盖行「启用这个」、赢家行「取消首选」；来源停用时客户端拦截报 `error.root.disabled`。校验故意宽松（避免脏声明名 fail-closed 整份状态文件），非法条目 `normalizeManagerState` 逐条丢弃。口径详见设计 §13.2。
3. **C6（实测抓出）**：`readDisabledTools` 正则把 `'*'` 静默丢弃 → 整台停用四链路空转，`src/index.ts` 放行（详见审查文档 §6.2）。

**收尾复验证据（本段新鲜运行，playwright-cli + 真实磁盘）**

- **复验①（来源停用时「启用这个」拦截）**：`sources.agents=false` 下点「公共 Agent」区被覆盖行（frontend-design）的「启用这个」→ 页面出现 `alert` 横幅「"公共 Agent"这个来源已整体停用，请先在该来源行启用它」，**无请求发出**；`state.json` md5 点击前后一致（`3f95255…26e14`）。**通过**。
- **七页页头同构核对**：子智能体（刷新/导入/新建人设）、提示词（新建预设/导入）、记忆（刷新/回收站/导入记忆/新建记忆）、会话（全选/导入对话/导出对话）、MCP（刷新/新增服务/全部启用/全部停用）五页均为「标题 + v0.2.0 徽章 + 单句描述 + 右侧动作组」（场景/技能两页上一段已核对）。**通过**。
- **测试数据恢复**：ui-test 场景经插件路径删除（确认弹窗 → `scene-memory/` 目录空、`rules-index.json` 回 `{archives:{}, active:null, mode:{scene:null,snapshot:null}}`）；`state.json` 手工恢复 `preferredSkills={}`、`enabledSkills.agents` 移除 find-extensions（`readManagerState` 每次调用从磁盘重读，无缓存回写风险，改动前已核实）。临时产物 `.pw-step.tmp.js`、`.playwright-cli/` 已删。
- 边界观察（如实记录，非缺陷）：残留 `preferredSkills` 指向已停用来源时，该来源条目仍赢同名分组且标「同名首选」、行状态如实显示「已停用」——键未失效只是来源关闭，符合设计 §13.2 口径；provider 侧生效判定不受该首选影响。

**自动化证据（复跑）**：`npm run build` / `npm run lint` 退出码 0；`npm test` **22/22**（archive 13 + import 9）；i18n zh/en 各 **412 键**无重复（临时脚本已删）。

**诚实边界（仍未验证，勿当已完成）**

- `subagent_run` **端到端未跑**（spawn 探测成功/失败分支、`[provider] <diagnostic>` 呈现、ask 门弹窗、场景绑定拒绝）。
- **bundle 导入未实现**（`rules-import` 只支持 `.md`/`.zip` 按 flat 落盘）；拖放实拖未实测（`droppedFiles` 已接线，只验过点击选择路径）。
- 引擎用例是假 deps 契约测试，替代不了真实写通道（C6 正漏在这层）；一次未被脚本触发的 `scene-mode-set(null)` 未定位（审查文档 §6.3）。
- 导入限额（8 MiB/文件、32 MiB/批、200 文件/批、2000 zip 条目）只在单测与代码层核对，未做大文件实测。

## 第四段：bundle 导入落地 + never 审批预检修复 + 拖放实拖（2026-09-13）

**做了什么**

1. **记忆导入支持 bundle**（`src/imports/upload.ts` / `src/rules/service.ts` / `test/import.test.mjs`）：zip 内 `<场景>/<名>/SKILL.md` 按 bundle 落盘，同层文件作附件平铺进同一目录；服务层按 `rules-attach` 同口径复检附件（名非法 / 空 / 单个 >8 MiB / 合计 >16 MiB → 逐条跳过并回报）。口径见设计 §14.1（修订 §12.2「本轮不支持 bundle」）。
2. **never 审批预检修复（用户报障 → 定位 → 修复）**：`tools/pre-execute` 里的 never 预检在完全权限会话中**恒为 false**，模型只收到通用「用户拒绝」。根因见下「根因链」；修复为 `ctx.get('approval')`，逻辑抽到 `src/approval-policy.ts`。口径见设计 §14.2（其后按用户裁定从「拒绝」改为「放行」，见下「口径修订」）。
3. **回归测试补齐 12 例**：新增 `test/approval-policy.test.mjs`（6 例，真实 cordis + 真实 `ApprovalService`）与 `test/subagent-scene.test.mjs`（6 例，场景绑定拒绝）；`npm test` 27 → **39**（一度 40，口径修订删掉 1 例 `neverPolicyHint`）。
4. **spawn provider 判断更正**：宿主**最新版自带** `spawn` provider（注册好的，无需 `npm i` 装包）。此前 README FAQ 把「spawn provider 不可用」写成普适故障——本轮改为**条件式**描述（只在缺包 / 特定 profile 形态出现）。
5. **拖放实拖**：记忆页导入弹窗对真实 `.zip` 走 `drop` 事件（非文件选择器），完成 UI → HTTP → 磁盘三段核对。

**根因链（never 预检为何恒 false）**

- 事实：会话 fold 为 `approval/policy: never`（完全权限预设写的是**会话级** fold，不是全局 `config.policy`）。
- 事实：修复前在本机以该形态调用 `subagent_run`，工具结果原文为 `Error: the user rejected tool "subagent_run"` —— 说明插件预检没命中，走到 `ask` 后被审批层判 `rejected`（fail-closed）。
- 定位：`(ctx as any).approval` 在本插件里**会抛异常**。`inject` 未声明 `approval`，cordis 上下文代理沿 fiber 链找不到 provider 时抛 `cannot get property "approval" without inject`（`@deepseek-ai/cordis@4.0.2` `ReflectService.handler.get`）；旧实现把它写在 `try/catch` 里，异常被吞成 `false`。
- 对照：`dsh-tools` 取这条缝用的是 `this.ctx.get("approval")`（`dsh-tools/lib/index.js` `serviceAsk`），且它的 `inject` 同样只有 `systemPrompt` —— 这也是「ask 卡在生产能弹出」的原因，可作旁证。

**证据（本段新鲜运行）**

- **拖放实拖 → bundle 落盘（三段交叉核对）**：
  1. UI：记忆页基线 `0 条记忆 / 0 个场景` → 「导入记忆」弹窗 → 向 dropzone 投递 `bundle验证.zip` → 显示「已选 1 个文件」、安装按钮由 disabled 变可点 → 安装后页头变 `2 条记忆 / 1 个场景`；列表出现「平铺记忆」（派生）与「bundle验证」（带 **bundle** 徽章）。
  2. 磁盘：`~/.dsh/scene-memory/导入验证场景/平铺记忆.md`（60 B）、`导入验证场景/bundle验证/SKILL.md`（71 B）+ `附件A.txt`（13 B）+ `附件B.png`（12 B）——**字节数与 zip 内条目逐一相等**；`rules-index.json` 登记 `导入验证场景/平铺记忆`、`导入验证场景/bundle验证` 两条 + 场景 `导入验证场景`。
  3. 注入链旁证：安装后同一进程的下一轮系统提示词里出现「场景记忆：导入验证场景」段（含两条正文）——落盘 → 发现 → 注入三段贯通。
- **never 预检（修复前，活体）**：会话 `session-cb97aed2` 日志含 `sandbox/mode {"mode":"danger-full-access"}` + `approval/policy {"policy":"never"}`；在该会话调用 `subagent_run`（人设名故意用不存在的 `__never_gate_probe__`，即使门失效也不会真起子代理、不烧 token）→ 返回通用 `the user rejected tool "subagent_run"`，**复现报障**。
- **never 预检（修复后，单测）**：`test/approval-policy.test.mjs` 7/7；第 1 例显式断言 `ctx.approval` 抛 `without inject` 而 `ctx.get('approval')` 正常解析——把根因锁进回归。
- **never 预检（修复后，活体；A/B 同会话对照）**：宿主重启（新进程 PID 31940 @ 20:20:17，监听 3080）载入带修复的 `lib/` 后，在**同一个** never 会话（`session-cb97aed2`，fold 仍为 `approval/policy: never`）复跑同一次 `subagent_run`：
  - 重启前（旧产物）：`Error: the user rejected tool "subagent_run"` —— 通用拒绝，正是报障现象；
  - 重启后（带修复）：`Error: 「运行子代理」的确认无法弹出：当前会话审批策略为 never（完全权限模式会自动拒绝一切需确认的操作，不会弹确认卡）。请把访问模式切换为「工作区内修改」后重试，或在插件设置里关闭对应确认开关（requireConfirmForModel*）。`
  - 该调用在预检处即被拒绝（未进入工具体、未起子代理），**未消耗额外模型调用**；两次对照的变量只有「宿主是否载入带修复的 `lib/`」。
  - ⚠️ 该「拒绝」口径**随后按用户裁定改为放行**（见下「口径修订」）——本条证据的价值在于：它证明 `isApprovalNever` 在该 never 会话里确实返回 true（否则不会产出 never 专属文案）。
- **场景绑定拒绝（单测）**：`test/subagent-scene.test.mjs` 6/6；关键断言：场景只绑 `other-helper` 时跑 `echo-test` → 抛「人设不可用: echo-test（当前启用场景的子智能体绑定: other-helper）」，且 `runSerial` **未被调用**（拒绝发生在运行之前，不白烧 token）。
- **自动化**：`npm test` **39/39**（archive 13 + import 14 + approval-policy 6 + subagent-scene 6）；`npm run build` / `npm run lint` 退出码 0。
- **环境实测更正**：`~/.dsh/settings.yaml` 的 `permission.defaultPreset: danger-full-access` 会让**新会话**自动带 `approval=never` fold —— 这是「完全权限」形态在本机极易复现的原因。
- **清理**：导入验证产生的 `scene-memory/导入验证场景/` 已删、`rules-index.json` 回基线（`{rules:{},groups:{},active:null,archives:{},mode:{scene:null,snapshot:null}}`）；临时 zip 与本地转发脚本只落在 `%TEMP%`（未进仓库）。

**口径修订（同日追加，用户裁定方案 B）：never = 预先批准，确认门直接放行**

- 触发问题：用户指出「DSH 在完全权限下本来就能起子代理」。核对属实——`dsh-tool-subagent`（官方 `subagent` / `subagent_fork`）**没有 ask 门**（该包内 `kind:'ask'` / `approval` / `pre-execute` grep 0 命中），完全权限下照常运行；只有本插件 `subagent_run` 被自己的确认门挡住，与预设自述 "without approval prompts" 和官方行为都不一致。
- 新口径：三个确认门（`rule_manager_write` / `skill_manager_create` / `subagent_run`）在 never 会话里**直接 `next()` 放行**，并写一条审计日志 `confirm-bypass` 留痕（`src/skills/service.ts` 新增 `pluginLog()` 单例，全插件共用一个串行写队列）。旧口径的 `neverPolicyHint` 文案随之删除（拒绝分支只剩「ask 但无 answerer」时宿主 `dsh-tools` 给的 `no approval channel is available`）。
- 这是**插件自定策略**，与宿主 `decide()` 的 never=自动拒 相反，已在设计 §14.2 写明理由与边界。
- 证据：
  - 两个半段各自已在本机活体验证过——① `isApprovalNever` 在该会话返回 true（旧口径据此产出了 never 专属拒绝文案，不是通用拒绝）；② 该处理器返回 `next()` 确实能让 `subagent_run` 走到底（临时把 `requireConfirmForModelSubagentRun` 置 false 实测：调用越过确认门、在查人设处报 `人设不可用: __never_gate_probe__（可用: (无)）`，零 token；随后已删除该设置文件回到默认）。
  - 自动化：`npm test` **39/39**（去掉 1 例 `neverPolicyHint`）；build / lint 退出码 0。
  - **新接线活体复跑（宿主重启后，同日 20:31）**：新进程 PID 24740 @ 20:30:48 载入方案 B 产物；在同一个 never 会话（`session-cb97aed2`）再调一次 `subagent_run`（人设名 `__never_gate_probe__`）→ 返回 `Error: 人设不可用: __never_gate_probe__（可用: (无)）`，即**已越过确认门、进入工具体**（旧口径此处是 never 专属拒绝文案）；同时 `~/.dsh/dsh-plugin-tool-management.log` 落入 `{"event":"confirm-bypass","detail":"完全权限（approval=never）：跳过「运行子代理」的确认，直接放行"}`（ts 12:31:18.273Z = 本地 20:31:18）。零 token（人设目录为空，未起子代理）。
- **spawn provider 归属（同日答用户）**：使用者**不需要**单独下载。① `@deepseek-ai/dsh-base` 的 `dependencies` 直接包含 `@deepseek-ai/dsh-subagent-spawn-in-process`，且 `dsh-base/cordis.patch.yml` 以 `providerName: spawn` 注册它 → 装标准 DSH 的用户开箱即有；② 插件还有第二道保险：`ensureSpawnProvider()` 探测不到就 `req()` 该包并 `apply(ctx, {providerName:'spawn'})`。只有「宿主未注册 **且** profile 内无该包」的畸形组合才会报错。

**验证矩阵（本段收口状态）**

| 验收项 | 方式 | 结果 |
|---|---|---|
| bundle 导入落盘（`<场景>/<名>/SKILL.md` + 附件） | 浏览器**实拖** zip + UI 计数 + 磁盘字节核对 | 通过 |
| 拖放路径（dropzone，非文件选择器） | 合成 `drop` 事件投递真实 zip | 通过 |
| 记忆注入链（落盘 → 注入） | 导入后系统提示词出现对应记忆段 | 通过 |
| 场景绑定拒绝「人设不可用」 | 单测 6 例（含 `runSerial` 未被调用） | 通过 |
| never 探测（修复前） | 活体：never 会话调用 `subagent_run` → 通用拒绝 | 已复现（bug 确认） |
| never 探测（修复后） | 单测 6 例（真实 cordis + `ApprovalService`） | 通过 |
| never 探测（修复后，活体） | 宿主重启后于同一 never 会话复跑 `subagent_run`（A/B 对照） | 通过（文字为旧「拒绝」口径，探测本身有效） |
| never 下确认门放行（方案 B 新口径） | 宿主重启后于同一 never 会话复跑 `subagent_run` + 审计日志核对 | 通过（越过确认门 → 人设不可用；`confirm-bypass` 已落日志） |
| `subagent_run` 成功分支（ask → 批准 → 真实运行） | 19:53 会话（用户提供，任务树有子代理条目） | 通过（沿用） |
| `subagent_run` 失败分支（spawn provider 缺失） | 本机宿主最新版自带 provider，构造不出该形态 | **N/A** |

**诚实边界（仍未验证，勿当已完成）**

- 方案 B 的活体验证首轮只覆盖 `subagent_run`（三个门共用同一处前裁决）；另两个门的具名复跑已在**第五段**补齐。
- 场景绑定的**活体验证**（真实 `subagent_run` 撞上绑定外调用）未跑，只有单测与工具可见性层证据；要跑活体需先建人设 + 只绑该人设的场景档案（第五段已备好测试人设，待宿主重载）。
- bundle 导入的各项限额已在**第五段**逐例实测（并抓出两处静默丢弃缺陷，已修）。
- 拖放实测首轮只覆盖单 zip、单批；**第五段**补齐多选混投与拖文件夹（后者用合成 entry，见该段边界）。
- 官方子代理工具「无 ask 门」是**源码级结论**（`dsh-tool-subagent` 内 grep 0 命中），未实跑（会真烧 token）。

## 第五段：限额 / 拖放 / 三个确认门的真实测试收口（2026-09-13）

用户要求「全部真实测试」，并把测试用模型钉在 `sensenova-6.8-flash-lite`。本段把第四段诚实边界里能跑的都跑掉，并**由实测抓出两处真实缺陷**（已修，见提交 `c441b63`）。

**做了什么**

1. **三个确认门具名复跑（never 会话，真写盘）**：`rule_manager_write` 与 `skill_manager_create` 各自实测放行并落盘，加上先前的 `subagent_run`，三个门全部具名验证；每次都在 `~/.dsh/dsh-plugin-tool-management.log` 留下 `confirm-bypass`。
2. **导入限额实测（7 例，直接打 HTTP op `rules-import`）**，并由其中 2 例抓出缺陷：
   - ✅ 单个上传文件 >8 MiB → `单个文件超过 8 MiB`；批合计 >32 MiB → `合计超过 32 MiB，其余文件已忽略`（同批还如实回报了 4 条 `正文超过 256 KiB`）；批内 >200 文件 → 导入 200 + `一次最多导入 200 个文件`；bundle 附件 >16 MiB → `附件合计超过 16 MiB，已跳过`；bundle 附件 >32 个 → `附件超过 32 个，已跳过`。
   - ⚠️ **缺陷 1（静默丢弃）**：zip 内单条目 >8 MiB 被 fflate 的 `filter` 丢掉却**不回报**——实测响应 `imported:["…/超大条目"]、skipped:[]`，磁盘只剩 `SKILL.md(22B)`，9 MiB 附件无声消失。
   - ⚠️ **缺陷 2（静默丢弃）**：zip 内条目 >2000 时，第 2001 条同样无声消失（隔离实测：2001 个非法名条目只回报 2000 条）。
   - 修复：`filter` 内补回报（条目数上限只报一次，避免 2000+ 条刷屏）。**修复后复测**：同一 zip 返回 `skipped:[{name:"复核场景/超限复核/huge.bin",reason:"zip 内单条目超过 8 MiB，已跳过"}]`。
3. **拖放实测（浏览器，真实 drop 事件）**：
   - ✅ **多选混投**（2 个 `.md` + 1 个 `.zip` 同批）→「已选 3 个文件」→ 安装后全局 2 条 + 场景「多选场景」1 条，注入可见。
   - ⚠️ **真实文件夹拖拽无法用 Playwright 合成**（`Dropping a directory is not supported — pass individual files`）。改为用**忠实的合成 `FileSystemEntry` 树**（含一层嵌套子目录）驱动真实代码路径，并如实标注：驱动的是插件代码，不是 Chromium 的真实目录拖拽。
   - ⚠️ **缺陷 3（目录前缀丢失）**：`readDroppedEntry` 会为文件夹拖拽写入 `_dssmPath`，但记忆/子智能体共用的 `ImportModal` 只取 `f.name` → 实测两条记忆落到**全局**而非 `文件夹测试/…`，与 zip 的「目录名当场景」口径不一致。修复：改用既有的 `uploadFilePath(f)`。**修复后复测**：`文件夹测试/夹内记忆.md` + `文件夹测试/子目录/深层记忆.md`，索引里出现多级场景 `文件夹测试/子目录`。
4. **人设支持 `provider`**（为满足「测试用 sensenova」而必须补的能力）：DSH 的 `resolveModel(provider, model)` 是**二元组**、不做 `provider/model` 字符串拆分，插件原先只传 `model` → 跨来源指定模型必然落到主会话的 provider 上并解析失败。现补齐 frontmatter 解析/序列化、`subagent-list`/`subagent-get`、前端表单与中英 i18n 各 3 键（避免「文件支持、UI 保存丢键」）。
5. **测试**：`npm test` 39 → **47**（新增 `test/subagent-persona.test.mjs` 6 例 + `test/import.test.mjs` 限额回报 2 例）；`npm run build`/`lint` 退出码 0；i18n zh/en 各 **416 键**、无重复、无单侧键（临时脚本核对后已删）。

**证据（本段新鲜运行）**

- `rule_manager_write` → `OK: rule 验证场景/never放行验证`，磁盘 `scene-memory/验证场景/never放行验证.md`（261 B）；`skill_manager_create` → `Created DSH skill never-bypass-probe at ~/.dsh/skills/never-bypass-probe/SKILL.md`（453 B），并即时进入会话技能目录。两件产物验证后已删、索引回基线。
- 限额 7 例的 JSON 应答逐条记录（见上），写入的 200 条测试记忆与 4 个测试 bundle 已全部清理。
- 拖放三次实测的 UI 文案与磁盘结构逐条记录（见上）。
- **首轮 `skill_manager_create` 曾报 `EPERM: rename … .dssm-create-<uuid> → never-bypass-probe`**，重试即成功、插件已自行回滚临时目录、目标目录无残留 —— 判为 Windows 瞬时锁（非插件缺陷），如实登记。
- **子代理端到端 + 场景绑定活体（宿主重启后，20:47–20:50）**：测试人设 `echo-test` / `other-helper` 均钉在 `provider: sensenova` + `model: sensenova-6.8-flash-lite`（密钥在 DSH 凭据库，非进程环境变量）。
  1. 建启用场景 `绑定验证`，档案只绑 `other-helper`（`archives.{"绑定验证"}.subagents=["other-helper"]`；`active:null` = 默认全部启用）。
  2. 可见性层：`subagent_list` → 只列 `other-helper`（`echo-test` 被绑定过滤）。
  3. **运行前拒绝**：`subagent_run{agent:"echo-test"}` → `Error: 人设不可用: echo-test（可用: other-helper）`（`reason` 仅在交集为空时给绑定清单，此处给可用名单，是设计行为）。
  4. **绑定内放行（真实运行）**：`subagent_run{agent:"other-helper", task:"绑定内放行"}` → 子代理真实运行并回传 `OTHER:绑定内放行`。
  5. **跨来源路由的决定性证据**：子会话 `e7b19d70…` 的 `request/header` 与 `request/context` 均为 `{"provider":"sensenova","model":"sensenova-6.8-flash-lite"}`（contextWindow 262144），`parentSession` = 本会话 `session-cb97aed2`、`subagent/descriptor` = `{mode:"one-shot", provider:"spawn", label:"other-helper"}`。
  6. **解除绑定后恢复**：清掉场景与档案 → `subagent_run{agent:"echo-test", task:"回声验证D"}` → `ECHO:回声验证D`。
  7. 清理：两个人设文件删除（`subagents/` 归 0）、场景目录与索引回基线、3 个测试子会话目录删除。
- **官方两个委派工具无 ask 门 + 本插件确认门弹卡（8123 宿主，`workspace-write` 会话，模型 `sensenova-6.8-flash-lite`）**：一条指令让该会话依次调 `subagent`（官方 spawn 委派）与 `subagent_run`（本插件），全程录屏式核对 UI 与会话日志。
  1. `subagent`：UI 显示「工具调用 subagent · 官方门验证」→ 子代理返回 `OK`；**全程没有出现审批卡**，会话日志里该调用之后**没有 `approval/asked`**。
  2. `subagent_run`：UI 弹出审批卡（详情为插件给的 `Run a subagent (consumes tokens)`，按钮「拒绝 / 允许一次」）→ 点「允许一次」→ 子代理返回 `ECHO:审批放行验证`；会话日志为 `tool/call subagent_run{agent:"echo-test"}` → `approval/asked {toolName:"subagent_run", reason:"Run a subagent (consumes tokens)"}` → `approval/decided {outcome:"allowed-once"}`。
  3. 任务管理树同时列出两个子代理条目（「官方门验证」「echo-test」，均一次性 · 空闲）；两个子会话的 `request/context` 均为 `{provider:"sensenova","model":"sensenova-6.8-flash-lite"}`（官方 `subagent` 子会话 `provider:"spawn"`、`subagent_fork` 子会话 `provider:"fork"`）。
  4. 补跑 `subagent_fork`（另开同配置会话）：同样无审批卡，子代理返回 `FORK-OK`；该会话日志里的策略 fold 为 `never → ask`（脚本切的预设），`tool/call subagent_fork` 之后无 `approval/asked`。

**验证矩阵（本段收口）**

| 验收项 | 方式 | 结果 |
|---|---|---|
| 确认门放行（`rule_manager_write`） | never 会话语义 + 真写盘 + 审计日志 | 通过 |
| 确认门放行（`skill_manager_create`） | 同上（含 EPERM 重试） | 通过 |
| 导入限额：8 MiB/文件、32 MiB/批、200 文件/批、bundle 16 MiB、bundle 32 个 | HTTP op 逐例实测 | 通过 |
| zip 丢弃条目回报 | 修复前静默（实测）→ 修复后回报（复测） | 通过（缺陷已修） |
| 拖放多选混投 | 真实 `drop` 事件投递 3 个文件 | 通过 |
| 拖文件夹 + 目录前缀保留 | 合成 `FileSystemEntry` 树驱动真实代码路径 | 通过（合成输入，非真实目录拖拽） |
| 人设 `provider` 跨来源路由 | 单测 6 例 + 真跑子代理并读子会话路由头 | 通过（`sensenova/sensenova-6.8-flash-lite`） |
| 场景绑定：可见性过滤 | `subagent_list` 于启用绑定场景下 | 通过（只列绑定内人设） |
| 场景绑定：绑定外运行前拒绝 | `subagent_run{echo-test}` | 通过（未起子代理） |
| 场景绑定：绑定内放行 + 解除后恢复 | 真实运行 `other-helper` / 清绑定后 `echo-test` | 通过 |
| 官方 `subagent` 无 ask 门 | workspace-write 会话（sensenova 模型）真实调用 | 通过（无 `approval/asked`，子代理返回 `OK`） |
| 官方 `subagent_fork` 无 ask 门 | 同上 | 通过（无 `approval/asked`，fork 子代理返回 `FORK-OK`） |
| 本插件 `subagent_run` 的 ask 卡 → 允许一次 | 真实弹卡 + 点「允许一次」+ 子代理运行 | 通过（`approval/asked` → `decided:"allowed-once"` → `ECHO:审批放行验证`） |

**诚实边界**

- 真实**文件夹拖拽**（Chromium 的 `webkitGetAsEntry` 目录 API）未被真实触发——Playwright 不支持；本轮用合成 entry 覆盖了插件侧代码。
- 官方两个委派工具与本插件确认门的活体验证见上，均在 **workspace-write（`approval=ask`）会话**里做。**官方工具在 `never` 会话下的行为未单独实跑**：这是「无 ask 门」的直接推论——`never` 只作用于 `ask`（`ApprovalService.decide()` 在派发 answerer 前判 never），无 ask 可判即无可拒；插件自身的三个确认门则走方案 B 直接放行。若要彻底闭环，可在完全权限会话里各跑一次（成本同本轮）。
- 清理时删除的测试子会话中含一个 `session-bf211678`（20:40 由 8123 宿主首次加载时自动创建的空会话），删除前未逐一核对内容——如实登记，该会话应为空。
