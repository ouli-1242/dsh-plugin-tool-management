# 2026-09-13 场景档案 v2 双轴评审（Standards × Spec）

**Status:** completed

- **对象**：分支 `fix/review-2026-09-13`，`git diff e4f71b7...HEAD`（HEAD=924b102，25 commits；代码轴 `-- src/ test/` 10 文件 +1327/−155）。
- **方法**：两路独立子代理评审——Standards 轴（Fowler 坏味道 + 项目六条 hard 约定）、Spec 轴（对照已批准设计/计划 + 官方 dsh-subagent / dsh-tools 类型逐字段核对）。主代理对两轴全部 Critical 与重要 Important **逐条源码抽查**（含官方类型在宿主安装目录的实地核对），合并裁决如下；两轴报告原文见文末第二节/第三节。
- **总裁决**：**有偏离** —— 合并 Critical 5 / Important 7 / Minor 12（轴报告原判：Standards 2C/3I/8m，Spec 4C/5I/7m；其中「空勾选集」两轴重叠，Standards I3 经设计核对后改判为 I7）。
- **诚实边界**：此前浏览器验收「进入模式 26→0→26」验证的是**快照可逆性**而非语义方向，因此未拦住 C1（方向反转）——方向类问题必须以设计条文为断言重测（设计 §7 测试计划第 1 条本就要求「核对启停已按档案生效」）。

## 一、合并裁决（去重 + 主代理实证后，按修复优先级）

### Critical（5，全部源码实证）

| # | 问题 | 来源 | 关键证据 |
|---|---|---|---|
| C1 | **MCP 档案勾选语义方向反转**：UI/设计说「勾=启用」，引擎把勾选集当作**停用名单**直写 sidecar | Spec C1 | `src/rules/archive.ts:74-82` computeMcpPlan 把勾选 spec 原样进 entries；`src/index.ts:355-362` applyMcpEntries 直写 DISABLED_TOOLS_FILE；UI 文案 `src/client.js:763/765`「勾选要启用的服务器」「勾选 = 该场景下启用」；设计 §2.1:52「勾 = 启用，未勾 = 停用」；测试名 `test/archive.test.mjs:14`「勾选服务器→停用名单」自证按停用集实现 |
| C2 | **模式进入缺「记忆启用集收窄为 {S, _shared}」步骤**，且代码注释/README 均声称已实现 | Spec C4 | 引擎 deps（`src/rules/archive-engine.ts:20-41`）无 active 通道，`scene-mode-set` 全程无 active 写；`src/client.js:1755/1763` 仅单次 op 调用无后续收窄；`src/rules/service.ts:574-579` resolveActiveScenes 只读 `index.active`；违反设计 §2.2:56③；`archive-engine.ts:2/:94` 注释与 `README.md:135`「记忆收窄到该场景」均为无实现支撑的声称 |
| C3 | **「段已定义但全不勾 = 全部停用」不可持久化** | Std C2 + Spec C2 | `src/rules/archive.ts:37` `if (list.length)` 丢服务器级空清单；`archive-engine.ts:79/:85` 删空 mcp/skills 段；与 `test/archive.test.mjs:11`（`hasSection(skills: []) === true`）自相矛盾；设计 §2.1:52 / §2.4:71 明文「全不勾 = 合法（全部停用）」 |
| C4 | **subagent 两工具绕过 defineTool 注册**，author 侧 parameters 未编译即入库（缺 top-level `type:'object'/properties/required`，且绕过 ToolArgsError 校验） | Spec C3 | `src/index.ts:2430-2431` 裸 `tools.register(defineSubagent*Tool(...))`；官方 `register()`（dsh-tools lib/index.js:2773 区段）仅校验 output 后原样 `layer.tools.insert`；本项目其余 12 处全部 `tools.register(defineTool({...}))`（`index.ts:2233-2411`），defineTool 负责编译（dsh-tools:1972 注释「defineTool compiles an object root」）；官方参照 dsh-tool-subagent 亦经 defineTool |
| C5 | **subagents.\* i18n 键全缺**（zh/en 均零定义），子智能体页整页渲染原始键名 | Std C1 | `t('subagents.*')` 实测 18 个不同键 / 20 处调用，DICT 定义数 0（脚本核对；Std 原报告记 21 系口径差，实质相同） |

### Important（7）

| # | 问题 | 来源 | 证据 / 备注 |
|---|---|---|---|
| I1 | **模式切换尾部提交 fail-open + 回滚不完整**：两 apply 成功后 `saveSlice` 失败则快照丢失不可逆；skills 应用失败只回滚技能不回滚已成功的 MCP；回滚失败被 `.catch(() => {})` 吞掉且返回信息谎报「已回滚」 | Std I1 + Spec I1 | `archive-engine.ts:143-144`（尾提交）、`:137-140`（部分回滚）、`:129/:139`（吞错） |
| I2 | **删除当前模式场景丢弃快照**，运行时启停永久停留档案态、无恢复路径 | Std I2 | `src/rules/service.ts:1762-1767`（注释自认防依赖成环的权衡，需给可逆出路） |
| I3 | **SubagentResult 诊断字段读错**：读 `result.detail`，官方字段为 `diagnostic` | Spec I2 | 官方类型（宿主安装 `~/.dsh/profiles/node_modules/@deepseek-ai/dsh-subagent/lib/types/types.d.ts:256` 区段）：`{ output, structured?, diagnostic?, stopReason }`；`src/subagents/service.ts:108` 读 `.detail` 恒 undefined，失败时模型只见空输出 + stopReason 前缀 |
| I4 | **`/scene-memory` 命令未按设计追加「当前模式与档案摘要」** | Spec I3 | `src/index.ts:2660-2676` 只输出记忆/场景启用状态；设计 §2.4:73 |
| I5 | **spawn provider 在场性偏离设计**：无「已注册则不挂载」探测、无 peerDependency 声明、cordis.patch.yml 无挂载声明、挂载失败静默吞掉 | Spec I4 | `src/subagents/service.ts:69-82`；`package.json:63-71` peerDeps 无 dsh-subagent-spawn-in-process；设计 §3.2:100 |
| I6 | **parseModeState v1 快照兼容分支死代码**：`snapshot.tools ? {} : {}` 两支相同，升级前数据退出模式会把「快照启停」还原成「全启用」 | Spec I5（Std 记 Minor，取严） | `src/rules/service.ts:500-502` |
| I7 | **引擎对「记忆 / 记忆+子智能体」型场景也收模式，超出设计**；UI 反而与设计一致 | Std I3 经设计核对后**改判** | 设计 §2.4:72 明文「只定义了记忆或记忆+子智能体的场景**不显示**模式按钮」——`client.js:1789` 只认 mcp/skills 是对的；过宽的是 `archive-engine.ts:113-117`（无 mcp/skills 段也以 snapshot:null 收模式） |

### Minor（12）

1. `src/subagents/tools.ts:53` 运算符优先级：`+` 先于 `||`，allowed 为空时丢右括号且 `(无)` 永不生效。
2. en 词典整块重复（`client.js:866-871` vs `:872-878`）；`memory.archive.emptySection` 仅 en 有（:877）且全文件无 t() 引用（死键），zh 缺失——「全不勾=全部停用」提示 zh 用户不可见；`addTools/addSkills/addSubagents` zh/en 都有（zh:771 / en:875）。
3. `src/index.ts:308` toolStates 不滤 `'*'` 产出 `server/*` 伪键（`:343` 滤了）；且 scene-inventory 的 `tools` 字段前端完全未消费。
4. `removeScene` 对 active/archives/mode 三次 read-modify-write 可合并为一次（`service.ts:1752-1767`）。
5. `client.js:2051` 死注释（引用已不存在的 modeInfo）。
6. 测试缺口：normalizeMcpSpec 畸形输入、空段语义（与 C3 联动）、configured 但未运行（knownTools 空）时 spec 工具不报 stale 的分支。
7. start 请求携带官方类型之外的 `provider` 键（官方 SubagentStartRequest 无此字段，`types.d.ts:136-148`），`signal: signal ?? undefined` 容忍官方必填缺省（`service.ts:92-101`）。
8. `exec.agent` 缺失时 undefined 直传必填 `parent`，无结构化错误（`tools.ts:55`、官方 `parent: Agent` 必填）。
9. 档案 subagents 段保存不做人设存在性校验（mcp/skills 都校验并报 stale，engine:74-86）。
10. `restoreSkills` 对 computeRestorePlan 已合并过的技能表再做一次同型合并（幂等无害，engine:47-53 + :105-108）。
11. 设计 §2.3:65 / §6:130 称 disabledTools sidecar「自带 .bak」失实——实际 `writeJsonFile` 无 .bak（`index.ts:355-362`），回滚仅靠内存快照。
12. spawnReady 一次性标记：挂载失败也置 true 不重试，且不校验 'spawn' provider 是否仍注册（`service.ts:69-84`，未逐行复核，按 Std 报告保留）。

### 最值得修的 3 件事（合并排序）

1. **C1 方向反转**（语义核心）——改 computeMcpPlan 产出**补集**：未勾服务器 → `['*']` 整台停；勾 `'*'` → `[]` 全启用；勾清单 → known−checked（未运行服务器 known 为空时尽力而为、靠 stale 上报兜底）；同步改测试名/断言；浏览器按「勾=启用」重测方向。
2. **C2 记忆收窄**——引擎新增 setActiveScenes 依赖通道（rules service 已有 patchIndex 可复用），进入模式在应用段之后写 `active=[S]`（resolveActiveScenes 自带 `_shared`）；退出按设计 §2.2:57 **不**恢复 active；子智能体绑定过滤（`index.ts:390` 按 active 并集）随之自动跟随模式。
3. **C3 + C4 + I1 打包小修**——引擎保留空段（normalizeMcpSpec 保留空清单，仅剔除全 stale 段）、subagent 两工具改经 defineTool 注册、尾提交改为先落盘后应用（或失败即反向恢复 + 回滚失败如实上报）。

（C5 的 18 个 i18n 键 + Minor#2 词典去重为机械活，随批修复。修复后按设计 §7 重跑浏览器方向断言：进入模式 → MCP/Skills 页核对「勾=启用」，含「只勾部分服务器/部分工具」与「全不勾=全停」两组用例。）

## 二、Standards 轴报告（子代理原文）

> 评审对象：分支 fix/review-2026-09-13 相对 e4f71b7 的功能 diff（src/ test/ package.json；docs 仅作上下文）。标准：Fowler 坏味道基线 + 项目六条 hard 约定。

**总评**：分层干净：纯逻辑（archive.ts）/ deps 注入引擎（archive-engine.ts）/ 域服务 / index.ts 装配四层各司其职，writeOps 派生落实了约定②并顺手修复了 rules-attach/detach/trash-remove 的门禁漂移；computeRestorePlan 对「快照后新增键保持现状」的处理正确，串行队列与外部 withWriteLock/skills 队列无死锁。但落地有三处硬伤：子智能体页 i18n 键全部缺失、空勾选集语义被引擎削掉、模式切换尾部提交存在 fail-open 窗口。前端拆分基本无残留交叉引用，仅注释级遗留。

**Critical**

1. **subagents.\* i18n 键全缺**（hard，违反约定⑤）——src/client.js:1949、1972 等 21 个键（subagents.title/desc/new/empty/create/edit/result.\*/delete.\*/field.\*）在 DICT.zh 与 DICT.en 中均无定义（已脚本核对：used 232 / zh 353 / en 361），t() 回退返回键名，子智能体页整页渲染原始 key。改法：zh/en 各补齐。
2. **空勾选集被静默删除，「段已定义但全不勾=全部停用」无法持久化**（hard，违反约定③与设计 §2.1 docs/设计/…-design.md:52）——src/rules/archive-engine.ts:79、85 删除空 mcp/skills 段（域变「不碰」）；src/rules/archive.ts:36-37 服务器级空工具清单同样丢键（未勾全部工具 → 应用后反而全启用）。冒烟测试 archive.test.mjs:11 断言的 hasSection(skills:[])===true 与引擎行为自相矛盾。改法：保留空段，仅剔除全 stale 段。

**Important**

1. **进模式尾部提交 fail-open**（hard，违反约定⑥）——src/rules/archive-engine.ts:140（await deps.saveSlice({ ...slice, mode })）：applyMcp/applySkills 成功后 saveSlice 失败则运行时已改、快照未持久化、不可逆；且 MCP/技能回滚失败被 .catch(() => {}) 吞掉（:129、:139）仍报「已回滚」。改法：回滚失败时把「回滚未成功」写进返回错误；saveSlice 失败时先尝试反向恢复。
2. **删除当前模式场景丢弃快照**（hard，约定⑥）——src/rules/service.ts:1762-1767 直接置 mode={scene:null,snapshot:null}，运行时启停永久停留在档案态；注释自认权衡但「重进任一模式」并不会还原进入前状态。改法：把 snapshot 转存到引擎可读的孤儿槽或提示用户运行时残留。
3. **UI/引擎不一致：仅绑 subagents 的档案无法进入模式**——src/client.js:1789 hasModeSections 只认 mcp/skills，忽略 archive.subagents；引擎（archive-engine.ts:116-121）支持仅 subagents 段入模式（仅记忆收窄+绑定过滤）。改法：`|| Array.isArray(archive.subagents)`。〔主代理注记：经设计 §2.4:72 核对，本条方向判定颠倒，已改判为合并 I7。〕

**Minor**（judgement call 除注明外）

1. 运算符优先级 bug——src/subagents/tools.ts:53：`+` 先于 `||`，allowed 为空时输出缺右括号且 (无) 永不生效。
2. parseModeState 死三元——src/rules/service.ts:502 snapshotRaw.tools ? {} : {} 两支相同，v1 快照静默丢弃。〔主代理注记：Spec 轴将同级问题列为 Important I5（升级数据还原成全启用），合并取严。〕
3. en 词典整块重复——src/client.js:872-877 与 866-871 重复定义 memory.mode/archive/result；且 memory.archive.emptySection 仅 en 有、无任何 t() 引用（死键）。
4. toolStates 与 serverKnownTools 的 '\*' 不一致——src/index.ts:308 不滤 '' 产出 server/\* 伪工具键，:343 滤掉；scene-inventory 的 tools 字段前端完全未消费（Speculative Generality）。改法：toolStates 同步滤 '\*'，或删 tools 字段。
5. spawnReady 一次性标记——src/subagents/service.ts:73-84：挂载失败也置 true 不重试，且不校验 'spawn' provider 是否仍注册（待确认）。
6. removeScene 三次 read-modify-write——src/rules/service.ts:1753-1767，可合并为一次。
7. MemoryPage 死注释——src/client.js:2051 仍称「并行 scene-mode-get 写入的 modeInfo/archives」，该页已不再调用且 state 无此二字段。
8. 测试缺口——test/archive.test.mjs 未覆盖：normalizeMcpSpec 畸形输入、空段语义、configured 但未运行（knownTools 空）时 spec 工具不报 stale 的分支。

**最值得修的 3 件事**：① 补齐 subagents.\* 的 zh/en 词典（用户直接可见的破页）；② 引擎保留空勾选段、normalizeMcpSpec 保留空清单，让「全不勾=全部停用」真正可保存；③ 修模式切换尾部提交 + removeScene 可逆性。

### 主代理核对注记（Standards 轴）

- C1：复核成立，实测 18 个不同键 / 20 处调用（原报告记 21 为口径差）；另 `memory.archive.emptySection` 仅 en 有且无引用、zh 缺失，`add*` 三键 zh/en 均有（zh:771 / en:875）。
- C2：复核成立（archive.ts:37 + engine:79/85 + test:11 矛盾）。
- I1：复核成立（engine:143-144 尾提交、:129/:139 吞错谎报）。
- I2：复核成立（service.ts:1762-1767）。
- I3：**改判**（见合并 I7）——设计 §2.4:72 明文此类场景不显示模式按钮，UI 与设计一致，引擎过宽。
- Minor 各条抽查属实（tools.ts:53、service.ts:500-502、client.js:866-878、index.ts:308、client.js:2051）；spawnReady（service.ts:69-84 区段）未逐行复核，按报告保留。

## 三、Spec 轴报告（子代理原文）

> 评审对象：git diff e4f71b7..924b102（src/ test/），对照 docs/设计/2026-09-13-场景档案与轻量子智能体-design.md 与 docs/计划/2026-09-13-场景档案与轻量子智能体.md。

### Critical

1. **MCP 档案段勾选语义与引擎应用方向相反（UI 说勾=启用，引擎把勾选写进停用 sidecar）**。computeMcpPlan（src/rules/archive.ts:77-81：`spec==='*'→entries=['*']`、`spec→entries=spec.slice()`，段内未提及的服务器 `entries=[]` 全保持启用）+ applyMcpEntries 写 DISABLED_TOOLS_FILE 停用表——勾选集被当作**停用名单**落盘；而 src/client.js:763/765 明示「勾选要启用的服务器」「勾选 = 该场景下启用；不勾 = 停用」，测试名（test/archive.test.mjs:14「勾选服务器→停用名单」）也证实按停用集实现。直接违反设计 §2.1 勾选集语义（design.md:52），且与同一档案弹窗内技能段语义（archive.ts:96-99 勾=启用）互反；另 src/client.js:1735 '\*'→明细转换以「当前 enabled 工具」预勾，在引擎方向下等于「把开着的应用为停用」，进一步印证 UI 与引擎各说各话。
2. **「段已定义但全不勾 = 全部停用」不可持久化**。src/rules/archive-engine.ts:79（mcp 空记录即 delete archive.mcp）、:85（skills 空数组即 delete archive.skills）——保存时空段被静默降级为「段未定义/该域不碰」，违反 design.md:52 与 §2.4（design.md:71）。
3. **subagent_list / subagent_run 以裸 ToolDefinition 注册，parameters 未经 defineTool 编译即发给模型 API**。src/subagents/tools.ts（parameters 为 author 侧 property-spec 形态）、src/index.ts:2430-2431（直接 tools.register）；官方 register() 不做转换、原样入库（@deepseek-ai/dsh-tools/lib/index.js:2773 区段），ToolSchema.parameters 契约是 raw JSON Schema（@deepseek-ai/dsh-llm types.d.ts:397-404），DeepSeek 适配器原样转发（dsh-llm-deepseek/lib/index.js:233）——模型收到的是无 type:'object'/properties/required 的非法参数声明，且绕过 defineTool 的 ToolArgsError 校验；官方参照实现 dsh-tool-subagent 均经 defineTool 注册。
4. **模式进入状态机缺少「记忆启用集收窄为 {场景, _shared}」步骤，且代码与文档声称已实现**。设计 §2.2 ③（design.md:56）明确要求；src/rules/archive-engine.ts 的 scene-mode-set 全程无任何 rules-index.active 写入（deps 亦无该通道），而 archive-engine.ts:2「进入 = 快照 → 应用已定义段 → 记忆收窄」、:94「仅记忆收窄的模式」注释与 README.md:135「进入模式 = …→ 记忆收窄到该场景」均为无实现支撑的声称（违反「证据先于声称」纪律）。

### Important

1. **fail-closed 回滚不完整**：进入模式时 skills 应用失败只回滚技能（src/rules/archive-engine.ts:138-140），此前已成功应用的 MCP 停用表不回滚——违反设计 §2.2「任一步应用失败 → 立即恢复快照」（design.md:60）；失败后运行时处于半应用态而 mode 仍指向旧场景。
2. **SubagentResult 诊断字段名错读**：src/subagents/service.ts:108 读 result.detail，官方字段为 diagnostic——非 completed 停止原因的 provider 诊断信息永远拼接不上，失败时模型只见空输出+stopReason 前缀。
3. **设计 §2.4「/scene-memory 命令输出追加当前模式与档案摘要」未落地**（design.md:73）：src/index.ts:2659-2674 命令处理未改动，仍只输出记忆与场景启用状态，无 mode/archives 摘要；实施计划亦未排期（计划静默丢弃设计需求）。
4. **spawn provider 在场性机制偏离设计 §3.2**：设计要求「运行时探测 provider、缺失才挂载 + peerDependency 声明 + 挂载声明进 cordis.patch.yml」（design.md:100）；实现为 createRequire 无探测无条件 apply 一次、失败静默吞掉（src/subagents/service.ts:69-82），cordis.patch.yml 无该包声明、package.json peerDependencies 无该包。当前宿主恰好自带该包故可运行，但存在双重挂载尝试与换宿主即静默失效的韧性缺口。
5. **parseModeState 的 v1 快照兼容分支是死代码**：src/rules/service.ts:500-502 三元两支均为 {}，v1 形态（snapshot.tools 布尔表）快照会被解析为空 MCP 停用表——若存在升级前数据，退出模式会把「快照启停」还原成「全启用」。

### Minor

1. toolStates 将停用表通配 \* 展开为伪 key `<server>/*` 混入 scene-inventory 的 tools 输出。〔主代理注记：实际位置 src/index.ts:305-316。〕
2. 英文 locale 重复定义同一批键（src/client.js:866 与 :872），且 memory.archive.emptySection（:871 区段）仅 EN 定义、全文件无引用；中文 locale 无对应键。
3. 设计 §2.3/§6 称 disabledTools sidecar「自带 .bak」（design.md:65,130），实际既有通道与新 applyMcpEntries 都走无 .bak/非原子的 writeJsonFile——行为未回归，但设计描述失实、回滚仅靠内存快照。
4. exec.agent 缺失时无结构化错误即把 undefined 传给必填的 SubagentStartRequest.parent（src/subagents/tools.ts:55；计划 Task 4 Step 2.2 明确要求「undefined 时报结构化错误」）。
5. start 请求携带官方类型之外的多余键 provider、signal 以 ?? undefined 容忍缺失（官方 signal: AbortSignal 为必填）。
6. 档案 subagents 段保存时不做人设存在性校验（mcp/skills 均校验并报 stale），悬空人设名可持久化（运行期绑定校验可兜底，故降级为 Minor）。
7. 退出路径 restoreSkills 对 computeRestorePlan 已合并过的技能表再做一次同型合并（幂等无害）。

### 已核对，符合（一行带过）

- 数据模型 v2：mcp 两级 + skills/subagents（archive.ts:10-13）；normalizeArchive 段键 null 视为未定义、客户端只发已定义段、服务端双保险（archive.ts:45-50、client.js submitArchive、设计 §10/§11）——符合（唯空段持久化见 C2）。
- op 名与协议：scene-\* / subagent-\* 齐备，rules-\* 内部协议保留，全部写 op 经 service 级 writeOps 派生进 WRITE_OPS——符合。
- 状态机其余步骤：进入先快照、仅应用已定义段、stale 跳过上报、切换先退后进、退出还原快照、同场景幂等、全程串行——符合（除 C4 与 I1 两点）。
- 非干扰红线（§9/§10）：系统提示词无新增/改动段（人设经官方 request.persona 注入）；MCP 停用只写插件自有 sidecar 并经官方 tools.restrict + guard 热路径；子代理只走官方 ctx.subagents.start；未触碰 DSH settings/会话/规则原生文件；cordis.patch.yml 未改动——符合。
- UI v2：七 tab、两级 MCP 勾选+「选工具」钻取+未运行标记、模式按钮显示逻辑——符合（语义方向见 C1）。
- B 轴其余：start('spawn', request) 双参签名；run.result 子代理失败不 reject；dispose() 在 finally；subagent_run 返回经 output.render 为 ContentBlock[] 数组形态；subagent_list 有可选参数避免空 schema；toolFilter:{allow} 匹配官方 ToolRestriction；agentOptions.model 合法；maxDepth:1；16KiB 截断；ask 门默认开、requireConfirmForModelSubagentRun 可关——符合（除 I2 与 Minor#4/5）。
- 计划符合性：hooks、新增斜杠命令、per-persona 工具注册、官方 agent-preset、tools/restrict 控人设可见性均未出现；计划交付物全部落地；S3 记忆继承有真实运行验证记录（docs/日志/2026-09-13-场景档案与轻量子智能体.md:26）——符合。

**VERDICT: 有偏离 — Critical 4, Important 5, Minor 7**

### 主代理核对注记（Spec 轴）

- 官方类型实地核对位置：插件 node_modules **无** dsh-subagent（宿主依赖，运行时经 createRequire 解析）；实际位于 `~/.dsh/profiles/node_modules/@deepseek-ai/dsh-subagent/lib/types/types.d.ts`（SubagentStartRequest :136、SubagentResult :256、SubagentRun :272 区段）。I2 成立：官方字段为 `diagnostic?`，`service.ts:108` 读 `detail` 恒空；Minor#5 成立：官方无 `provider` 字段、`signal: AbortSignal` 必填。
- C3（defineTool）独立复核**成立并加强**：index.ts:2430-2431 裸注册 vs 全项目其余 12 处 `tools.register(defineTool({...}))`（index.ts:2233-2411）；官方 register()（dsh-tools lib/index.js:2773 区段）仅校验 output 后原样 insert；dsh-tools:1972 注释确认 defineTool 负责编译 object root。注：「参数实际不可见」未做线上抓包复核——实际表现受模型按 description 推断与 API 宽容度掩盖（浏览器冒烟中两工具可被正确调用），但契约偏离成立。
- C4（记忆收窄）独立复核成立，证据链见合并表 C2（含 README.md:135 复核）。
- 行号修正：Minor#1 toolStates 实为 src/index.ts:305-316；C1 中 applyMcpEntries 实现在 src/index.ts:355-362（引擎侧为 deps 接口声明 archive-engine.ts:31-32）。
- dsh-llm types.d.ts:397-404、dsh-llm-deepseek:233 两处引用未逐行复核（C3 结论不依赖它们）。

## 四、抽查方法附记

- 官方类型核对：`C:/Users/ouli/.dsh/profiles/node_modules/@deepseek-ai/dsh-subagent/lib/types/types.d.ts`（经 `SubagentStartRequest/SubagentResult/SubagentRun` 逐字段比对）。
- dsh-tools 注册链：`node_modules/@deepseek-ai/dsh-tools/lib/index.js` register（:2773 区段，仅校验 output 原样 insert）与 defineTool（:837 定义、:1972 编译注释）。
- i18n：grep 统计 `t('subagents.*')` 18 键 20 处 vs DICT 定义 0；memory.archive.\* 相关键 zh/en 分布逐一定位。
- 设计条文：design.md §2.1:52 / §2.2:56-57,60 / §2.3:65 / §2.4:71-73 / §3.2:100 / §6:130 逐条对照实现。

## 五、修复登记（2026-09-13，同分支 `fix/review-2026-09-13`）

**状态**：C1–C5 / I1–I7 / Minor 12 全部处置；1 条设计字面偏离（I5，用户已批准默认方案）。

| 条目 | 处置 | 落点 |
|---|---|---|
| C1 | `computeMcpPlan` 改产**补集**（未勾服务器 → `['*']` 整台停；勾 `'*'` → `[]`；勾清单 → `known − 勾选`） | `src/rules/archive.ts` |
| C2 | 引擎进入模式第 ③ 步写 `active = [S]`（`_shared` 由 resolveActiveScenes 恒常加入；退出不恢复）；`patchIndex` / `readArchiveSlice` 增 `active` 通道 | `archive-engine.ts`、`rules/service.ts` |
| C3 | 引擎保空段（仅「全 stale」丢段）、`normalizeMcpSpec` 保空清单、`normalizeArchive` 段存在性不变 | `archive.ts`、`archive-engine.ts` |
| C4 | `subagent_list` / `subagent_run` 改经 `defineTool()` 编译注册（参数 object root/required 由 defineTool 生成） | `src/index.ts`、`subagents/tools.ts` |
| C5 | zh/en 各补 21 个 `subagents.*` 键 | `src/client.js` |
| I1 | 进入顺序改「快照 → **先落盘 mode** → 应用 → 收窄」；失败回滚覆盖 MCP+技能+切片，回滚失败写进错误文本（不再谎报「已回滚」）；退出失败保持原 mode 不变 | `archive-engine.ts` |
| I2 | `rules-remove-scene` 拒绝删除当前模式场景（`error.rules.sceneInMode`，双语键），并给出可逆出路 | `rules/service.ts`、`client.js` |
| I3 | 诊断字段改读官方 `result.diagnostic` | `subagents/service.ts` |
| I4 | `/scene-memory` 追加当前模式 + 档案摘要 | `src/index.ts` |
| I5 | **有意偏离**：不写 yml 挂载行，改官方 `ctx.subagents.list()` 探测 → 缺失才 `apply(ctx,{providerName:'spawn'})` → 失败原因原样抛出；`package.json` 加**可选** peerDependency；yml 内写明取舍 | `subagents/service.ts`、`cordis.patch.yml`、`package.json` |
| I6 | `parseModeState` 的 v1 布尔表折算为 v2 停用名单（不再静默丢弃） | `rules/service.ts` |
| I7 | 仅记忆 / 仅子智能体场景进入模式 → 结构化拒绝（与设计 §2.4:72 及 UI 一致） | `archive-engine.ts` |
| Minor 1 | 运算符优先级（可用名单右括号 + `(无)` 生效） | `subagents/tools.ts` |
| Minor 2 | en 词典去重；`memory.archive.emptySection` 补 zh 并接上 UI（空段三处提示） | `client.js` |
| Minor 3 | `toolStates` 不再产 `server/*` 伪键（整台停用在 live 工具上展开）；`scene-inventory` 增 `allToolsDisabled`，`tools` 字段前端消费于预勾 | `src/index.ts`、`client.js` |
| Minor 4 | `removeScene` 索引清理合并为一次读-改-写 | `rules/service.ts` |
| Minor 5 | MemoryPage 死注释订正 | `client.js` |
| Minor 6 | 测试缺口 3 个补齐（normalizeMcpSpec 畸形输入/空段语义/未运行时 stale 口径） | `test/archive.test.mjs` |
| Minor 7 | 去掉非官方 `provider` 键；`signal` 缺省补永不中止的 AbortSignal | `subagents/service.ts` |
| Minor 8 | `exec.agent` 缺失 → 结构化错误 | `subagents/tools.ts` |
| Minor 9 | 档案 subagents 段保存时对人设全集校验并报 stale（与 mcp/skills 同口径） | `archive-engine.ts`、`src/index.ts` |
| Minor 10 | 随状态机重写：`restoreSkills` 双合并与 `computeRestorePlan` 一并删除，还原走单一 `restoreSnapshot` | `archive.ts`、`archive-engine.ts` |
| Minor 11 | 设计 §2.3/§6「sidecar 自带 .bak」失实 → 就地订正；并回填退出还原口径与 spawn 挂载偏离 | `docs/设计/…design.md`、`README.md` |
| Minor 12 | spawnReady 一次性标记 → 每次调用先探测；挂载失败原因带出、不重试性保留在 `spawnMount` 缓存 | `subagents/service.ts` |

**评审之外的补充（本次一并处置）**：

1. `normalizeMcpSpec` 丢服务器级空清单（用户复核发现）：已保留，UI 回读保真（「未勾」≠「勾了 0 个工具」）。
2. 「+ 添加 MCP 工具集」原本建 `mcp: {}`，修好 C1 后等于「全部服务器停用」：改为按当前运行时状态预勾（`mcpPreset()`，整台停用/全不启用 → 不勾）。
3. **新发现（本次）**：旧的「快照后新增键保持现状」合并规则会让**模式自己写入的** `server: ['*']` 在退出后残留——用户环境退出后仍被静默停用。改为退出/回滚统一按快照**原文**整体回写 MCP 停用表（`computeRestorePlan` 删除），技能仍只写快照列出的键。设计 §2.2 已回填口径。
4. **浏览器实测抓出的 C6**（`['*']` 整台停用从未生效，解析层过滤掉了通配）——修复与证据见 §6.2。

**验证证据（可复跑）**：

- `npm run build` → `tsc -p tsconfig.json && node scripts/sync-client.mjs`，退出码 0（defineTool 的参数契约进编译期检查）。
- `npm run lint` → `node --check lib/client.js && node --check lib/index.js`，退出码 0。
- `npm test` → **13/13 通过**（原 5 个用例按新语义改判 + 新增 3 个纯逻辑缺口用例 + 5 个引擎状态机用例：空段持久化、补集应用+记忆收窄、仅记忆场景拒绝、失败回滚、回滚失败如实上报）。
- i18n 覆盖核对（临时脚本，跑完即删）：zh/en 各 376 键、无重复键、21 个 `subagents.*` 双语齐备、`memory.archive.emptySection` 双语且被 UI 引用。

**诚实边界（未验证项，勿当已完成）**：

- **浏览器实测已于同日补跑**（`dsh web` + playwright-cli，证据见下方「浏览器验收」）：上表前端条目已从代码级核对升级为实机行为证据。**但 `subagent_run` 端到端仍未跑**（人设目录为空）：spawn provider 的探测成功/失败分支、ask 门弹窗均未验。
- 引擎状态机用例是**注入假 deps** 的契约测试，不能替代真实写通道的行为验证——浏览器实测抓出的 C6 正好落在假 deps 覆盖不到的 sidecar 解析层。
- 一次**未被任何脚本触发**的 `scene-mode-set(null)`（16:45:06 本地）未定位，见下方「未定位的一次性事件」。

## 六、浏览器验收（2026-09-13，`dsh web` + playwright-cli）

方法：`dsh web --no-open --port 8123`（web profile 经 junction 挂载本开发目录）+ playwright-cli 驱动真实 UI；每一步同时回读三处**权威状态**：UI 渲染文本、浏览器同源 API（`scene-mode-get` / `scene-inventory`）、磁盘 sidecar（`~/.dsh/dsh-plugin-tool-management-disabled-tools.json`、`~/.dsh/tool-management/rules-index.json`）。

### 6.1 实测结果（全部通过）

| 验收项 | 结果 | 关键证据 |
|---|---|---|
| ①「勾=启用」方向（只勾 github） | ✅ | 磁盘 `{github:[], tavily:['*'], context7:['*'], playwright:['*'], serena:['*'], fastgraph:['*']}`；运行时 github on:26 / tavily off:5 / context7 off:2；5 台未勾服务器 `allToolsDisabled:true` |
| ② 勾服务器 + 只勾部分工具 | ✅ | 钻取取消 `create_or_update_file`、`search_repositories` → 档案存 24 个；重进模式后停用名单**恰为那两个**（known − 勾选），运行时 github on:24 / off:2 |
| ③ 全不勾 = 全部停用 | ✅ | 档案 `mcp:{}` **未再被丢段**（持久化成功）；进入后 6 台全 `['*']`，github off:26（旧代码此处会「全部启用」） |
| ④ 退出模式完全还原 | ✅ | 退出后 `disabled={}`、全部服务器 on、`mode=null`；两次进/退结果一致，无残留 |
| ⑤ 仅记忆 / 仅子智能体场景 | ✅ | 卡片无「设为当前模式」按钮；直连 API 返回 `{ok:false, error:"场景「pw-memo」的档案没有 MCP / 技能段（仅记忆或仅子智能体的场景不需要进入模式）"}` |
| ⑥ 模式中删除场景 | ✅ | UI 确认删除后报「该场景正处在当前模式，请先退出模式再删除」，场景保留 |
| ⑦ 记忆收窄 | ✅ | 进入后 `active:["pw-test"]`；场景页该场景无「未启用」标记、另一场景显示「未启用」；退出后 `active` **保持**（符合设计 §2.2） |
| ⑧ 子智能体页 i18n | ✅ | 整页中文文案，`subagents.*` 原始键名出现 **0** 次 |
| ⑨ 空段提示 + 添加段预勾 | ✅ | 点「+ 添加 MCP 工具集」预勾 **6/6（全部工具）**（旧行为是空段）；取消全部后显示「该段已定义但没有勾选任何条目 = 全部停用」 |

### 6.2 实测抓出的新 Critical（编号 C6，已修）

**C6：`['*']`（整台服务器停用）从未真正生效。** `readDisabledTools` 解析 sidecar 时用 `/^[A-Za-z0-9_-]{1,128}$/` 过滤服务器级名单，`'*'` 不匹配 → 被静默丢弃。连带失效四件事：① guard 热路径（`isToolDisabled` 查不到 `*`）；② `applyToolRestrictions` 的整台展开；③ 进入模式的**快照保真**（快照取自同一解析结果）；④ 勾选器 `allToolsDisabled` 标记。**修好 C1 后新语义完全依赖它**（未勾服务器写的就是 `['*']`），不修则「未勾 = 整台停用」空转。

- 修复前证据：磁盘已写 `tavily:['*']`，但 `scene-inventory` 报 `tavily on:5 / off:0`、`allToolsDisabled:false`。
- 修复：`src/index.ts` 解析处放行 `'*'`（一行 + 注释）；重建后同一操作实测 `tavily:ALL_OFF` + off:5、context7 off:2、github on:26。
- 结论：**这条是纯自动化测不到的**——引擎契约测试注入假 deps，绕过了 sidecar 解析层；只有真实宿主 + 真实磁盘能暴露。

### 6.3 未定位的一次性事件（如实记录，勿当已解决）

16:45:06（本地）出现一次**未被任何脚本触发**的 `scene-mode-set(null)`：表现为模式自动退出、技能快照还原（35 条 `policy-*` 日志）、MCP 停用表回写为空。已排除的触发源：进程启动时间（16:46:40）晚于该时刻 → 非重启触发；客户端 `scene-mode-set` 仅两处调用点（进入/退出按钮）。候选解释：自动化点击与 React 重渲染的竞态，或人为点击（浏览器窗口可见）。事后「进入 → 静置 25s」重复 1 次未复现。**建议：下一轮专门用「点击后立刻断言 mode 未被退出」的脚本跑 5 次以定性。**

### 6.4 本轮未覆盖

- `subagent_run` 端到端（spawn provider 探测成功/失败分支、`[provider] <diagnostic>` 呈现、ask 门弹窗、场景绑定拒绝）——人设目录为空，未跑。
- 服务器「后加载」场景（进模式后才启动的服务器是否被 `['*']` 拦住）——未跑。

### 6.5 测试数据

pw-test / pw-memo 已删除；`disabled={}`、`archives={}`、`mode={scene:null,snapshot:null}`、`active=null`、`scene-memory/` 为空——与实测前基线一致（已核对）。

### 6.6 收尾复验（2026-09-13 同日，交接清单执行）

| 复验项 | 结果 | 证据 |
|---|---|---|
| 来源停用时「启用这个」拦截（§5 前端条目的实机补验） | ✅ | `sources.agents=false` 下点「公共 Agent」区被覆盖行（frontend-design）「启用这个」→ `alert` 横幅「"公共 Agent"这个来源已整体停用，请先在该来源行启用它」；`state.json` md5 点击前后一致（`3f95255…26e14`），无请求发出 |
| 七页页头同构（补看其余五页） | ✅ | 子智能体=刷新/导入/新建人设；提示词=新建预设/导入；记忆=刷新/回收站/导入记忆/新建记忆；会话=全选/导入对话/导出对话；MCP=刷新/新增服务/全部启用/全部停用——均「标题 + v0.2.0 徽章 + 单句描述 + 右侧动作组」 |
| 测试数据恢复 | ✅ | ui-test 场景经插件路径删除（确认弹窗）；`scene-memory/` 空、`rules-index.json` 回 `{archives:{}, active:null, mode:{scene:null,snapshot:null}}`；`state.json` 恢复 `preferredSkills={}`、`enabledSkills.agents` 移除 find-extensions（`readManagerState` 每次调用从磁盘重读，手改无缓存回写风险） |
| 临时产物清理 | ✅ | `.pw-step.tmp.js`、`.playwright-cli/` 已删除 |

边界观察（如实记录，非缺陷）：残留 `preferredSkills` 指向已停用来源时，该来源条目仍赢同名分组并标「同名首选」，行状态如实显示「已停用」——键未失效、只是来源关闭，符合设计 §13.2 口径；provider 侧生效判定不受该首选影响。
